# ABOUTME: 可持续披露报告编制工作台后端包根。
# ABOUTME: 覆盖契约、收资、生成、导出、账户与持久化六层；数据模型见 docs/schema-contract.md。
# ABOUTME(en): Backend package root of the sustainability disclosure report authoring desk.
# ABOUTME(en): Spans contract, intake, generation, export, accounts, persistence; data model in docs/schema-contract.md.
