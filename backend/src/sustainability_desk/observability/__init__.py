# ABOUTME: 链路阶段观测包，拥有 open_stage 这一唯一开启阶段的出口。
# ABOUTME: 本包只负责阶段结构与归属校验；LLM 调用事实仍由 llm/ai_observability.py 拥有。
# ABOUTME(en): Pipeline-stage observability package, owning open_stage as the only way to open a stage.
# ABOUTME(en): Owns stage structure and ownership checks only; LLM call facts stay in llm/ai_observability.py.
