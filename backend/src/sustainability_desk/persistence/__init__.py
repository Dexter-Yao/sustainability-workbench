# ABOUTME: 持久化层——Supabase(PostgreSQL) 连接池、持久化配置与数据访问；契约结构不入库，只存用户状态快照与审计证据。
# ABOUTME(en): Persistence layer — Supabase (PostgreSQL) pool, settings and data access; contract
# ABOUTME(en): structures are not stored, only user state snapshots and audit evidence.
