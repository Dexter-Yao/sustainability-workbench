# ABOUTME: 暴露结构解析工具的可重建阅读投影；现有 target-centric 检索仅供旧审阅链，禁止晋升为生产语义门。
# ABOUTME: 本包不拥有原文件事实；File Agent 目标路径只按需复用其中的结构与 Markdown 工具资产。
# ABOUTME(en): Exposes reconstructible reading projections of structural parsing tools; retrieval is legacy only.
# ABOUTME(en): This package owns no source-file facts; the File Agent path reuses its structure and Markdown assets.
