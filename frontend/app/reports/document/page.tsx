// ABOUTME: 报告正文路由 /reports/document：生成成功后阅读、逐段修订整份报告并查看逐块来源；交互全部在客户端组件。
// ABOUTME(en): Report document route; reading, per-paragraph editing and provenance live in the client component.

import { ReportDocumentPage } from "@/components/report-document/report-document-page";

export default function DocumentPage() {
  return <ReportDocumentPage />;
}
