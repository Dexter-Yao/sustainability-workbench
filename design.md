<!-- ABOUTME: 设计系统唯一视觉真相源（design.md）——原则、基础令牌、布局骨架、组件、内容编辑模型、文案、页面注册表、治理。 -->
<!-- ABOUTME: 所有前端 UI 的统一依据。结构借鉴 Material 3（Foundations → Tokens → Components → Patterns）。视觉真相源；数据/行为真相源是 docs/schema-contract.md。 -->

# 设计系统（design.md）

> **定位**：整个 Web App 的**视觉与交互唯一真相源**。所有前端界面、组件与设计稿须与本文对齐；冲突以本文为准。
 > **基调**：分步准备 + 生成后的报告正文页——任务优先、内容优先、扁平克制、贴近正式中文文档，弱化界面外壳（chrome）。
> **与数据契约的关系**：业务/数据/行为真相源是报告 Schema（`docs/schema-contract.md`、`backend/data/*.yaml`、`frontend/lib/schema.ts`）。本文只约束「界面看起来怎样、怎么交互」，**不承载业务语义或状态机定义**，但视觉必须忠实反映 Schema 的概念（blockType、state、source）。
> **范围**：规定原则、令牌（token）、骨架、组件原则、编辑交互模型、文案与命名。不逐像素规定每个组件；细节在实现时按本文原则确定。本文为长期文档（evergreen）。
> **结构来源**：组织方式参考 Material 3 的「Foundations → Styles/Tokens → Components → Patterns」分层。

---

## 0. 产品定位与调性

本产品是面向中国市场的 ESG（环境、社会、治理）报告生成工具。轻量版服务中小企业和非专业用户，
以低负担准备、自动生成和 Word 交付为主：通过分步准备完成输入，报告正文页是生成成功后的阅读与修订面，
不是首次生成的必经界面。

视觉调性关键词：**干净、专业、严肃、克制、可信、中文优先、零技术门槛**。

可信赖性是第一位的视觉目标。在严肃合规场景下，一致、克制、毫不浮夸本身就是信任信号；任何为「好看」而增加的装饰都会削弱专业感。同时，因用户无技术背景，**直观（不用思考）压倒一切聪明**：能用颜色/位置表达的，绝不用文字解释。

---

## 1. 设计原则

 1. **任务与内容优先**：分步准备让当前任务和下一行动成为主角；报告正文页让报告正文成为主角。界面外壳
   （顶栏、侧栏、工具栏）视觉权重尽量低，不与用户当前目标竞争。
2. **克制用色**：以中性灰为主体，主色（松墨绿）只用于少量可操作要素与活跃态；单屏彩色面积约 ≤5%。
3. **层级靠排版，不靠装饰**：信息层级用字号、字重、留白表达，不靠彩色块、边框、阴影堆叠。
4. **中文长文档优先**：行高、行宽、两端对齐、全角标点服从正式中文文档的阅读习惯。
5. **状态即信任**：每个操作的完成、生成、待确认、阻断、错误都有明确、一致的视觉反馈；块的状态一律走视觉指示（边线/底色/角标/字色），**绝不把状态写进报告正文内容**。
6. **颜色即权限**：在正文里，**能不能改一眼可辨**——墨色可改、灰色锁定、绿色为回填字段，无需文字说明（见 §5）。
7. **键盘是加速器，不是门槛**：可提供快捷键，但每个动作都必须同时有可点击按钮；说明集中在固定位置、默认隐藏。
8. **术语完整专业**：界面用语说完整话、用全称（「缺少资料」而非「缺料」、「影响、风险与机遇（IRO）表」而非裸 IRO），与数据契约术语一致。

---

## 2. 基础 Foundations

采用**语义双层令牌**：界面只引用语义层；语义层在亮色取下表值，暗色经 `.dark` 重映射（§2.9 预留）。

### 2.1 色彩 · 语义令牌（亮色）

```css
:root {
  /* 表面与文字（暖石中性，略偏绿灰，与主色同源） */
  --background:        #FFFFFF; /* 主内容区背景 */
  --surface:           #F6F7F5; /* 页面底层背景 */
  --surface-sunken:    #EDF0EC; /* 侧栏、次级容器背景 */
  --foreground:        #19211D; /* 正文主色 / 可编辑墨色（不用纯黑） */
  --foreground-secondary:#3C443E; /* 次级正文：图表/表格说明、tooltip 深字（比 foreground 浅、比 muted 深） */
  --muted-foreground:  #677069; /* 辅助文字、说明、表头 / 锁定灰 */
  --border:            #E2E6E1; /* 分隔线、输入框边框（极浅） */
  --border-strong:     #C7CCC6; /* 三线表横线等强分隔 */

  /* 主色：松墨绿 Pine */
  --accent:            #2C6E49; /* 主操作、活跃态、进度、焦点环 */
  --accent-foreground: #FFFFFF; /* 主色之上的文字 */
  --accent-hover:      #1F4D33; /* 悬停 / 按下 / 深色绿文字 */
  --accent-subtle:     #E7F0E9; /* 活跃项底色、焦点块弱底 */
  --accent-subtle-border:#CFE2D5;
  --ring:              #2C6E49; /* 键盘焦点环 */

  /* 字段回填高亮（预览与编辑两侧统一；见 §5.3） */
  --field-foreground:  #1F4D33;
  --field-background:  #EAF2EC;

  /* 语义状态 */
  --success:           #2C6E49; /* 完成 / 已就绪（= 主色：完成即绿） */
  --warning:           #B27513; /* 待确认 / 待核实 */
  --destructive:       #BC3B2E; /* 错误 / 缺少资料 / 破坏性操作 */

  /* AI（与「人写内容」区分；仅用于生成态指示，不入正文） */
  --ai:                #6A55C8;
  --ai-subtle:         #ECE9FA;

  /* 双重重要性矩阵象限底色（数据分区用，非品牌色；与后端 matrix_chart._QUADRANT_BLUE 同值） */
  --chart-quadrant:    #3D7CC9;
}
```

**用色规则**
- 主色只出现在：主操作按钮、活跃导航项、键盘焦点态、进度指示、回填字段高亮（绿）。其余一律中性。
- **禁止渐变**——合规场景传递的是精确，渐变带来营销感。
- 语义色（红/橙/紫）**禁止出现在报告正文区**，只用于状态反馈元素与编辑器信号。
- **字段回填一律绿色高亮**（`--field-*`），预览侧与编辑侧完全一致；**不得用黄/琥珀表示「待填」字段**。黄/琥珀（`--warning`）只表示「待确认/待核实」状态，不用于字段。
- 状态标签（Badge）用语义色约 10% 透明度作底 + 饱和文字，不用实心色块，避免「交通灯」压制正文。
- 中性灰统一取「暖石」色阶（略偏绿灰），与松墨绿同源；需要更细灰阶时在该色系内取，不引入冷 Slate。中性文字分三档：`--foreground`（正文/可编辑墨）> `--foreground-secondary`（次级正文、图表说明）> `--muted-foreground`（辅助/锁定灰）。
- `--chart-quadrant`（蓝）是**唯一获准脱离松墨绿体系的颜色**，且只用于双重重要性矩阵的四象限底色与其图例色块。它承担的是数据分区语义而非品牌语义：象限底色若取绿色，会与主色承担的「主操作/活跃/完成」语义混淆，也会与该图上按分类着色的散点争夺注意力。不得挪作他用，也不得因「与品牌色不一致」把它改成绿色——同源约束见下条。

### 2.1.1 浮层阴影令牌

所有浮层一律使用 §2.5 已定义的 `--shadow-overlay`，不另设浮层阴影令牌；引用未声明令牌由 §9.2
规则②机器检查捕获，不依赖人工评审。

### 2.1.2 字号令牌（补齐并封闭）

命名沿用既有 `--text-*-size` 约定：

```css
:root {
  --text-title-size:      20px; /* 页面主标题（h1）、抽屉标题 */
  --text-section-size:    16px; /* 区块小标题（h2）、对话框标题 */
  --text-subsection-size: 15px; /* 次级小标题、必填星标 */
  --text-body-size:       14px; /* 界面正文、所有输入控件 */
  --text-label-size:      13px; /* 字段问题、分组小标题、caption */
  --text-supporting-size: 12px; /* 帮助说明、元信息、后果型备注 */
  --text-overline-size:   11px; /* overline、状态标签 */

  --text-glyph-size:      16px; /* 图标按钮字形（⋯ / ×）；非文字层级，不当标题用 */
}
```

**封闭规则**：说明式文字只能取 `--text-label-size` / `--text-supporting-size` /
`--text-overline-size` 三阶之一；界面正文与输入控件固定 `--text-body-size`；
标题取 `--text-title-size`（20px，页面 h1 与抽屉标题）/ `--text-section-size`（16px，区块小标题、
对话框标题）/ `--text-subsection-size`（15px，次级小标题与必填星标）三阶之一。
另有 `--text-glyph-size`（16px）**只服务图标按钮里的字形**（`⋯` / `×`）——它的尺寸由点击目标与
视觉重量决定，与文字层级无关，故独立成阶，不得拿来当标题用。

**报告正文是独立排版面，不进以上任何一阶**：serif / 15px / 1.95（§2.2 字号表「报告正文」行，
实现在 `components/report-document/document-nodes.tsx`）。它与 `--text-subsection-size` 恰好同值是巧合——
共用令牌会让调界面次级标题时连报告正文一起改掉，故刻意保留字面量并在检查中显式登记豁免。

认证面（`app/login`、`components/auth/*`）使用 Tailwind 字号类而非 `fontSize` 令牌，是 §9.2
字号检查覆盖不到的独立通道，是否并入令牌体系待定；在此之前不单方面迁移。

**代码中不得出现 `fontSize` 数字字面量**（图表 SVG 内部标签与报告正文 serif 排版面除外，见下）。
两条机器检查分别执行：11–14px 带由 §9.2 检查③ 执行、有只许收缩的豁免清单；
15px 及以上的标题带**无豁免**，新增即红。
封闭字号阶是消除「12px 与 13px 在同类字段之间随机漂移」的前提。

### 2.1.3 浮层、字段状态与语义底色

```css
:root {
  --hint-surface:       var(--background);   /* 提示浮层底色：必须是白，不是 --surface */
  --hint-foreground:    var(--foreground-secondary);
  --topbar-height:      52px;                /* 全局顶栏；左栏 sticky 偏移由它派生 */
  --field-empty-border: var(--border-strong);/* 未填输入的边框 */

  /* 语义状态派生底：10% 底用于 Badge，6% 底用于 InlineAlert 大面积 */
  --warning-subtle:       #F7F0E2;
  --warning-surface:      #FDFAF3;
  --destructive-subtle:   #F7E7E5;
  --destructive-surface:  #FDF6F5;
  --success-surface:      #F4F9F5;
}
```

- **`--field-empty-border` 的用意**：未填状态用**加深边框**表达，不用主色淡底——4% 主色在
  白底上几乎不可辨，且绿色底在 §5.3 已被「字段回填」语义占用，遵守「一物一义」。
- 「生成中」语义复用既有 `--ai` / `--ai-subtle`，不另设 `--info` 平行令牌。

### 2.2 字体与中文排版

**字体栈（系统字体优先，不加载 CJK Web 字体）**——中文 Web 字体动辄数 MB，是真实性能负债；系统字体可读性最优、与 Word 导出一致。

```css
--font-sans:  -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC",
              "Microsoft YaHei", "Noto Sans CJK SC", sans-serif;     /* 界面 */
--font-serif: "Songti SC", SimSun, "Noto Serif CJK SC", serif;        /* 报告正文 */
--font-mono:  ui-monospace, "SF Mono", "Cascadia Code", Consolas, monospace; /* 标识符/数值 */
```

- **界面用无衬线，报告正文用衬线（宋体系）**——衬线呼应正式文档与 Word 导出语义，并在视觉上把「正文」与「界面外壳」分开。
- 等宽仅用于代码、标识符、对齐数值（启用 `font-feature-settings: "tnum"`）。**装饰性 mono 标签禁用**——小标题/overline 用 11px 无衬线。

**字号与行高**

| 层级 | 字号 | 行高 | 字重 | 字族 | 用途 |
|---|---|---|---|---|---|
| Display | 30px | 1.3 | 600 | sans | 营销/封面级标题（罕用） |
| H1 | 24px | 1.4 | 600 | sans | 章节主标题 |
| H2 | 20px | 1.45 | 600 | sans | 节标题 |
| H3 | 16px | 1.5 | 600 | sans | 子节标题 |
| 界面正文 | 14px | 1.7 | 400 | sans | 表单、说明、界面文字 |
| 报告正文 | 15px | 1.95 | 400 | **serif** | 报告主体内容 |
| 辅助 | 13px | 1.6 | 400 | sans | 说明、表注、时间戳 |
| 标签 | 11px | 1.5 | 500 | sans | Badge、overline、状态 |

全局最高字重 **600**，不用 700（界面发硬）。

### 2.2.1 说明式文字角色（三级语义分级）

界面上一切「非报告正文」的说明式文字——字段问题、帮助说明、图表表注、字数计数、进度元信息、界面提示——须按语义角色分三级，用**字号 × 字重 × 中性色三档**（§2.1）区分，杜绝「一律 12px + 灰」的无分级平铺。零新增颜色令牌。

| 级 | 角色 | 字号/字重 | 色令牌 | 位置 | 标记 |
|---|---|---|---|---|---|
| 一 | 字段问题 / 输入标签 | 13 / 500 | `--foreground` | 控件正上方独占一行 | 无 |
| 一 | 分组小标题 | 13 / 600 | `--foreground` | 分组顶部 | 无 |
| 二 | 帮助说明（hint） | 12 / 400 | `--muted-foreground` | 标签或控件下方 | 无 |
| 二 | 后果型备注 | 12 / 400 | `--muted-foreground` | 独立行 | 左 2px 细规，或前置 ⓘ |
| 二 | 图注 / 表注（caption） | 13 / 400 | `--foreground-secondary` | 图表上/下方 | 无 |
| 三 | 计数 / 进度元信息 | 12 / 400 | `--muted-foreground` | 右对齐或独立行 | 无 |

**归位判据**

三级分级表本身不变，但某段文字落在哪一级由下面这条**可机械执行的判据**决定：

> **判据：这段文字是不是「填对这一项所必需的」？**
>
> - **是** → **必须常驻页面**，与字段同屏可见，不得折叠。
> - **否** → **折进 ⓘ**（见 §2.8.1），让页面保持干净。

判据是文字的**性质**，不是长度。

**必需 · 常驻页面（四类）**

1. **填什么**——字段要的是哪个事实（「与营业执照登记名称一致」）。
2. **怎么填**——格式、单位、取值范围、字数上下限。
3. **条件依赖**——填了 A 才需要 B；三项须齐备才生效。
4. **直接后果**——不填 / 填错会发生什么（「内容原样进入报告」「不填则该段落自动省略」）。

**非必需 · 折进 ⓘ（三类）**

1. **背景与依据**——为什么要问、依据哪份准则。
2. **系统内部如何使用**——这项会进入哪个章节、参与哪步装配。
3. **术语解释**——「财务重要性」「双重重要性」是什么意思。

**文案风格合同**

说明式文字的读者是**缺少 ESG 专业训练的企业经办人**，不是外行，也不是我们自己。

- **专业、简洁、克制**：一眼读完即懂，不写口水话，也不堆砌术语。
- **直接写给用户**：禁止第三人称与作者视角措辞（「用户……」「这里需要用户选择」）。
  内部编写痕迹（署名、待办、编辑指令）一律不得出现在用户可见字段。
- **两段各司其职**：常驻段说「这一项要填什么、怎么算」；ⓘ 段说术语区分与口径依据。两段不得复述同一件事。
- **外部来源按需引用**：只在确实决定「该填哪个数」时引用（如排放因子取生态环境部公布值）。
- **准则条款有确切把握才标**：标错条款比不标更糟；无从核实的编号一律不写。

**长度不能推翻性质**

必需信息即使有 80 字，也不许折进 ⓘ——正确做法是**拆**：按字段拆成各自的一句 hint、改写成
结构化清单、或把条件依赖做成动态显隐。反过来，非必需信息即使只有 6 个字，也不该占正文行。

**不说明也是一种归位**

标签已经说清楚要填什么时，**不写说明**——把标签换个说法重说一遍（「报告期起始」下写
「报告期起始日期」）只会稀释旁边真正有信息量的说明。同理，**默认值已经填在框里时不必用文字复述**。

因此 `InputGuidance.helpText` 可缺省：一个条目可以只为 `defaultRule` 或 `termExplanation` 存在。
判据仍是性质而非篇幅——**必需信息不得因为「短」而省略**，省略的前提是标签已经承担了它。

**说明写不顺，有时是字段本身不该存在**

删不掉又写不清的说明是一个信号：先回头问这一项是否该收集，而不是继续打磨措辞。典型情形是
问了一个编制期无解的问题（如尚未发布的报告链接）。删字段的判据是：**该输入是否改变任何交付物**。
不改变的，删；界面上少一次选择，胜过留一个填了也看不见效果的框。

**排版约束**（在性质判断之后才适用）

- **常驻说明位于标签与控件之间**：说明是「动手之前要知道的事」，读完再落笔；
  放在控件下方会让用户填完才看见。报错则相反——它针对已经填进去的内容，留在控件下方。
  二者不再共用槽位，报错出现时说明不被顶掉。
- ⓘ 浮层内容不超过 80 字 / 3 行。超限说明它可能其实是必需信息，重新判性质。
- **同一字段可同时提供 inline hint 与 ⓘ，但两者必须各承载一类信息**：inline hint 常驻，
  只放必需信息（填什么 / 怎么填 / 条件依赖 / 直接后果）；ⓘ 折叠，只放非必需信息（术语解释 /
  背景与依据 / 系统如何使用）。**被禁止的是把必需信息挪进 ⓘ、以及两者复述同一件事**，
  不是并存本身：强制二选一会迫使「术语与准则背景」要么挤进常驻行、要么整段丢弃。
- **一级最该被读到**：字段问题以墨色（`--foreground`）13px 中字重与其下方的灰色说明拉开层级；不得用 `--muted-foreground` 呈现字段问题。
- **三级为元信息**：字数计数、次数、时间戳等，`--muted-foreground` 12px，**右对齐或独立成行**，不加标记；越过下限/上限时字色转 `--destructive`。
- **字数计数**取「已输入 / 上限」纯计数对；未达下限时以极短提示追加（「至少 N」），不用长括号句式。同一类输入的计数呈现须一致（前四章与议题内容清单不得一有一无）。

**中文排版规则**
- 报告正文行高 1.95、两端对齐（`text-align: justify`）、全角标点（，。；：「」）。
- 段落首行缩进 2em 与段间距两方案**不混用**（正文取首行缩进）。
- 中文正文**禁用 `letter-spacing`**；仅纯英文/数字的 overline 可用宽字距。
- 报告正文列宽约 **680px** 居中（每行 28–38 字最易读），不铺满全宽。

### 2.3 间距与栅格

8px 基准，4px 半步；间距取自固定阶梯：`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64`。杜绝随机 padding——一致的间距节奏本身就是信任信号。

### 2.4 圆角 Shape

| token | 值 | 用途 |
|---|---|---|
| `--radius-control` | 6px | 按钮、输入框、小控件 |
| `--radius-container` | 12px | 卡片、浮层、容器 |
| `--radius-pill` | 999px | 标签、状态点、进度条 |

正文内容块**不加圆角容器**（见 §8）。

### 2.5 层级与投影 Elevation

仅**浮层**允许「突起」；内容块之间用留白分隔，不用边框/阴影围合。

| 层级 | 阴影 | 用途 |
|---|---|---|
| 卡片 | `0 1px 2px rgba(25,33,29,.05)` | 罕用；产品内内容块不套卡片，用留白替代（§8） |
| 浮层 | `0 6px 20px rgba(25,33,29,.12)` | 下拉、气泡、浮动工具条 |
| 弹窗 | `0 16px 48px rgba(25,33,29,.18)` | 模态对话框 |

z-index 固定分层：内容 < 固定栏 < 浮层/下拉 < 弹窗 < 全局提示（Toast）。

### 2.6 动效 Motion

动效只服务反馈与状态变化，不做装饰性炫技；一律遵守 `prefers-reduced-motion`，
降级为瞬时切换。时长分层，令牌在 `globals.css :root`，不得各处散落字面量：

| 层 | 用途 | 时长/形态 |
|---|---|---|
| 微过渡 | 控件 hover、进度条、图例过滤淡出 | 120–250ms `ease`/`ease-out` |
| 元素入场 `--motion-enter-item` | 新散点落位（`gs-matrix-point`）、就地提示（`gs-alert-enter`）、对话框打开（`dialog[open]`） | 200ms `ease-out`，挂载一次性 |
| 区块入场 `--motion-enter-section` | 完成时刻：生成交付区升页首（`gs-delivery-enter`）、评分完成矩阵强调（`gs-matrix-reveal`） | 350ms `ease-out`，挂载一次性 |
| 状态循环 | 进行中脉冲 `gs-pulse`、coach 光晕 `gs-halo` | 1.6–2s `ease-in-out` infinite |

两条纪律：**一次性入场只在真实状态转变时触发**（触发条件是代码侧状态比较，
直达终态的用户不被重复打扰）；**重渲染不重放**（动画挂在不换 key 的元素上）。

AI 生成态：柔和底色高亮 + shimmer 骨架 + 三点脉冲（紫）。

### 2.7 无障碍 Accessibility

- 文本对比度 ≥ WCAG AA（正文/`--foreground` 对 `--background` 远超 4.5:1）。
- 状态**不只靠颜色**：辅以图标、文字或形状（如「待确认」环、「缺少资料」叹号）。
- 键盘可达：所有可操作元素有可见 `--ring` 焦点环；命中区 ≥ 44px（触控）/ ≥ 32px（鼠标）。
- 模态抽屉须声明对话框语义、打开时将焦点移入、在内部约束 Tab、支持 `Esc` 和遮罩关闭，并在关闭后恢复触发控件焦点。

### 2.8 图标 Iconography

线性、克制、单色（继承文字色）；禁用彩色/拟物/插画图标。状态点优先用纯色圆点/环，不滥用图标。

### 2.8.1 提示图标 Hint（ⓘ）组件契约

唯一实现位于 `components/ui/Hint`。**开合矩阵**（同一组件按 `@media (hover: hover)` 分支，不做两个组件）：

*指针设备*

| 动作 | 触发 |
|---|---|
| 打开 | `mouseenter`，延迟 120ms（避免划过误触发） |
| 打开 | 图标获得焦点（`onFocus`，键盘 Tab 或点击后聚焦均触发；**不是 `:focus-visible` 语义**），无延迟 |
| 关闭 | `mouseleave`，延迟 80ms（允许指针划入浮层） |
| 关闭 | `blur` |
| 关闭 | `Esc`，焦点在页面任意位置均生效 |
| 关闭 | 祖先容器滚动，立即 |

*触摸设备（`hover: none`）*

| 动作 | 触发 |
|---|---|
| 打开 | 点击图标（命中区扩至 44px，视觉仍为 15px） |
| 关闭 | 点击页面其他任意位置 |
| 关闭 | 再次点击图标 |
| 关闭 | 滚动，立即 |
| 关闭 | 同页另一个 Hint 打开（全局单例） |

**视觉**

- 图标：15px 圆，1px `--border-strong` 描边，`--background` 底，`--muted-foreground` 字，内容为 `i`。
- 悬停/打开态：描边与字转 `--accent`，底转 `--accent-subtle`。
- 键盘焦点：`--ring` 焦点环，2px / offset 2px。
- 浮层：`--hint-surface` 底 + 1px `--border` + `--radius-container` + **`--shadow-overlay`**；
  `--hint-foreground` 字，`--text-supporting-size`，行高 1.65，最大宽度 270px。
- 命中区 ≥32px（指针）/ ≥44px（触摸），透明扩展，视觉仍 15px。
- 浮层默认图标下方左对齐；靠近视口右缘时翻转为右对齐。过渡 `opacity .12s ease`。

**无障碍**

- 图标 `aria-label="查看说明"`；浮层 `role="tooltip"`，开态由**图标自身**的 `aria-describedby` 引用。
  `Hint` 的 `id` 参数原意是供字段控件引用，但当前无任何字段消费它——**这是已知无障碍缺口**：
  屏读用户在字段上不会听到 ⓘ 内容，须先 Tab 到图标。接入字段侧引用前不要把本行读作已实现。
- 浮层内**不得放链接、按钮或表单控件**。需要交互的是 Popover，不是 Hint。

**禁止**

- 只有点击一种开合路径。
- 把「填对这一项所必需的信息」放进 ⓘ，无论它有多长（见 §2.2.1 判据）。
- 用原生 `title=` 属性代替（延迟不可控、样式不可控、触摸端不可达）。
- 让 ⓘ 与常驻 inline hint 复述同一件事（并存本身允许，但必须各承载一类信息，见 §2.2.1 排版约束）。

### 2.9 暗色（预留）

令牌为双层结构，暗色仅需在 `.dark` 重映射语义层。初期只支持亮色（中文长文档白底黑字最优、与 Word 一致），结构预留、不暴露切换入口。

### 2.10 品牌标识

产品名由 `frontend/lib/product-name.ts` 单点声明；当前只用文字字标（`BrandMark`），图形标识待定：

- 产品 UI、Word 封面与 Excel 收资表不放任何图形标识；顶栏与登录页品牌位以文字字标呈现，字体走 §2.2 的界面字体栈。
- 不引入品牌专用字体进入产品 UI；产品 UI 以 `--accent: #2C6E49` 为主色，遵守「禁止色彩漂移、禁止渐变」原则。
- 图形标识定稿后在本节登记资产位置与使用规则，前端与导出侧再据此接入，不得先于本节改代码。

## 3. 布局与导航骨架 Scaffold

> **边界：** §3.1 定义主旅程，§3.2–§3.4 定义生成后报告正文页的布局与边界，不是轻量版首次
> 生成旅程。轻量版主路径是“报告列表 → 分步准备（scope-aware）→ 资料处理 → 显式生成 →
> `/reports/generation` 进度与交付 → 报告正文页阅读、修订与导出”；页面只消费服务端 typed projection，
> 不自行推断资料分类或运行阶段。

### 3.0 全局导航锚点 AppTopBar

所有登录后页面共用同一副骨架，任何一屏都能回答「我在哪、还剩多少、怎么回去」。
**AppTopBar** 是所有登录后页面的共同祖先，高 `--topbar-height`（52px），`--background` 底 +
底部 1px `--border`，`padding: 0 20px`。

槽位与真相源：

| 槽位 | 内容 | 数据来源 |
|---|---|---|
| 左 · 品牌 | 文字字标（§2.10，`BrandMark`）18px，点击回报告列表 | 静态 |
| 左 · 报告名 | 当前报告显示名，**纯文本**——不承载切换/新建下拉（报告操作在列表页与右侧动作区） | `reportDisplayTitle()` |
| 右 · 动作 | **无**。建报是列表页的职责，编制中的页面不需要常驻建报入口；「我的报告」去向在左栏报告组（见本节「报告组」） | — |
| 右 · 账户 | 登录身份文字——显示完整邮箱，不用头像图标；菜单只含账户身份块（组织名称若有 / 邮箱 / 权益）与「退出登录」——不承载「内部审计包」与「重新开启引导」 | `useAccount().snapshot` |

**顶栏不承载步骤条与保存态。** 编制步骤导航是外壳左栏的固定竖排导航
（`IntakeStepRail`，报告上下文页渲染；报告正文页用章节树目录）：
overline「编制步骤」+ 编号圆点 + 步骤名，当前步 `--accent-subtle` 底高亮。
步骤圆点：18px 圆；已完成 `--accent` 底 + 白 `✓`；当前 `--accent` 底 + 白数字；
未开始 `--surface-sunken` 底 + `--muted-foreground` 数字。
**步骤数量必须来自 `getIntakeSteps(scope)`**，不得硬编码；每段状态读服务端
`preparation.areas[].status`，不由浏览器推断。**步骤页标题（h1）与布局遥测屏标签派生自
`intakeStepLabel()`**（与步骤声明同文件，定量步标题按 scope 投影）：页面与布局不双写标签；
遥测屏 id 是独立稳定标识，不随标签漂移。
**二级标题**：当前所在步骤展开其页面区块的二级条目——锚点链接 +
必填/选填标记（必填 `--destructive` 字、选填 `--muted-foreground` 字），让用户在导航面一眼
看到本页哪些必填、哪些选填，并可一键跳转。二级条目的事实源归
`frontend/lib/intake-info-sections.ts`（区块 id、标题、义务），页面 SectionTitle 上的
必填/选填标记与左栏同源于它，不得双写。只在合适的页面展示：当前仅基本信息步开放，
其余步骤的区块结构定稿后再逐一登记。
**报告组**：步骤组上方另设 overline「报告」组，承载两个报告级去向：
顺序为「我的全部报告」（→ `/reports`）在前、「当前报告交付物」（→ `/reports/generation`）在后——
从账户全部到当前一份，与用户心智顺序一致。**两条只有标签、不带说明行**：标签本身已自解释，
左栏每条再挂一行说明只是噪音。当前所在页以 `aria-current` 与主色底高亮。
分隔线下接「编制步骤」组。报告列表页本身即列表，无左栏的生成交付页以
页内面包屑（我的全部报告 / 报告生成与交付）承担返回入口。两个条目的标签、说明与去向的唯一
事实源是 `frontend/lib/report-navigation-labels.ts`：编制页左栏报告组、报告目录常驻入口、
生成交付页面包屑、报告列表页标题（「我的全部报告」）与生成交付页标题
（`REPORT_GENERATION_PAGE_LABEL`）同源于它，不得在消费侧双写。

**「新建报告」点击即创建服务端报告**，不存在「填入主体名称才落库」的草稿模式：
报告列表页的「新建报告」按 `createReportWithDefaults` 创建报告并写入合同模板的输入默认值，
随后设当前报告指针进入分步流第一步——不存在无报告的编制页。创建期间按钮置灰（「正在创建…」），
失败原因如实展示（网络、达上限、会话过期），不静默。未填主体名称的报告在列表回落显示
「{级别}报告草稿 · {创建日}」（`report-display-title`），填入后派生「{级别}报告 · {主体名称}」。

自动保存态渲染在内容区右上，随内容面而非顶栏（锚点 `#coach-autosave`）。文案由
`save-state-label` 单源判定，**四态且冲突态优先级最高**：冲突未解（「未保存：本页修改尚未写入，
请先加载最新内容」，`--destructive` + `role="alert"`）→ 保存中 → 自动保存已开启 → 已自动保存，
后三态用 `--text-supporting-size` / `--muted-foreground`。
**保存态是用户判断「我填的东西在不在」的唯一常驻依据，不得报喜不报忧**：冲突暂停时若仍显示
「已自动保存」，用户会继续填写并全部丢失。

**「继续上次的编制」绑定账户而非设备**：`/` 与列表行「继续」都落到该报告**当前该继续的
那一步**。目标步骤取服务端 `preparation` 自带的 `href`（先取第一个 `needs_input` 区块，其次首个
阻断项），浏览器不维护第二份 area→路由映射。都没有时落到**末步生成区所在页，末步由
`primary_input_mode` 判定**（questions→议题信息填写、materials→资料处理、未选择→定性信息
gateway）——不得按 `areas` 末位推算：materials 区块无条件产出且恒在末位，照末位推会把直答路径
的用户送到不在其步骤序列里的 `/materials`。本机指针（localStorage）只是同设备快捷方式：缺失或
指向已删除报告时，按服务端事实重建为最近编辑的活跃报告（`updated_at` 是权威事实）。
报告列表取不到时不猜，回落到 `/reports` 由该页的门禁与错误提示处理。

**外壳收敛为两套**

| 外壳 | 用于 | 构成 |
|---|---|---|
| `AccountAccessFrame` | 登录 | 无顶栏，400px 居中单列 + 文字字标（`BrandMark`，回报告列表） |
| `AppShell` | 报告列表 · 全部准备步骤 · 资料处理 · 生成与交付 · 报告正文 | `AppTopBar`（常驻）+ 可选左栏 + 内容区 |

报告列表页与生成交付页同样纳入 `AppShell`，但不显示左栏步骤导航
（前者尚无当前报告，后者不依赖报告编辑上下文）；生成交付页以页内面包屑承担「我的全部报告」
返回入口。

**报告目录只在报告正文页渲染。** 章节条目是页内锚点，编制步骤页、交付页与列表页都不渲染章节树；
未就绪引导入口（§3.3）同样只属于报告正文页——在没有正文可去的页面留一个章节标题就是留一个死胡同。

**不设独立引导页**。`/` 无当前报告时重定向到 `/reports`；有当前报告时落到该报告当前该继续的那一步（同上「继续上次的编制」）；
流程说明是报告列表页的常驻侧栏（空状态时展开，步骤名读 `getIntakeSteps(scope)` 动态渲染），
用户任何时候回到列表都能重看。

**侧栏只列步骤名，不写时长估计，也不写副说明**（见 §7.2 不承诺无法预测的量）。
可跳过性不用「本步可跳过」标签预告——由步骤页说明行承载（如评分页「不评分时报告将完整覆盖
全部适用议题」）；必填性由左栏步骤导航的红色「必填」标记表达（步骤 `required` 声明在
`lib/intake-steps.ts`，是步骤必填性的唯一登记处）。

### 3.1 主旅程与报告正文页分层

首次生成不在报告正文页采集输入。前三步固定，第 4 步起按**报告级填报方式二选一**分岔。
该步步骤名为**「定性信息」**，与第 3 步「定量信息」相对应：议题的定性内容由「上传现成资料」
或「直接回答问题」二选一提供，标题旁带「二选一」备注（两条路径互斥，必须明示）：

```text
报告列表 → 分步准备（1.基本信息 → 2.重要性评分 → 3.定量信息）
  → 未选择：4.定性信息（/intake/input-path，二选一 gateway）
  → materials 路径：4.上传资料 → 5.资料处理 + 页尾生成区
  → questions 路径：4.议题信息填写（/intake/questions，含图片素材区）+ 页尾生成区
  → 进度与交付 → 报告正文页（阅读、修订、溯源、导出）
```

**填报方式（步骤名「定性信息」）是报告级二选一**：「上传现成资料由 AI 解析」或「直接回答议题引导问题」。选择是报告级
事实，落 `StoredReportStateV4.meta.primaryInputMode`、经服务端 preparation 投影（`primary_input_mode`）下发，前端不得本地推断；它只改
分步流编排，**不清空、不迁移任何已填输入**。**写入后必须回读服务端权威 state 并整体替换本地基线**
（`getReportState` + `applyAuthoritativeReportState`）：该端点只返回资料工作区快照、不含完整
`StoredReportState`，只同步 `state_seq` 会让下一次自动保存以陈旧基线把 `primaryInputMode` 覆盖回
null，preparation 随即认为用户从未二选一，「生成前需要确认」区与门禁一并失效。服务端拥有字段的
保全是写入层锁内事实（`backend/tests/test_reports_persistence.py`），不靠页面写法钉住；字段所有权的
声明与保全规则归 `docs/schema-contract.md`。

**旁路写入的两条通则**：凡绕过 `PUT /api/reports/{id}/state` 主通道、直接改写报告持久化状态的
端点，其调用方都受这两条约束——

1. **基线必须与服务端写入等价**。`applyServerReportUpdate` 省略第三参数的前提是「updater 已把
   服务端本次写入的每一个字段都回放进运行时 Report」：投影会从 Report 重新生成 state，凡未回放
   的字段都会被下一次自动保存按陈旧基线覆盖回旧值。答不上「这次改了哪些字段、是否逐个回放」
   就传完整 state 或改用 `applyAuthoritativeReportState`。`stored-report-state` 中无条件取自
   baseline 的字段（`companyBusinessSummary`、`meta.primaryInputMode`）对基线陈旧零容忍。
2. **派生投影必须随触发它变化的事实重取**。preparation 一类服务端派生投影（门禁、完成度、
   `primary_input_mode`）一律经 `useReportPreparation(reportId, refreshToken)`
   消费，令牌至少含 `savedAt`；直接调 `fetchReportPreparation` 并只依赖 `activeReportId` 会让
   生成区门禁与问答页必答区停在挂载那一刻的判定。同屏两个组件消费同一投影时
   刷新节奏必须一致，否则左右两栏各执一词。

两条路径的事实（intakeItems 与资料证据轨）并存，生成侧允许任一为空，已上传资料在 questions
路径下仍参与生成。已选择后步骤条只显示所选路径，更改入口是**所选路径每一页页顶**的
「更改填报方式」低调链接（supporting 字色行内 text link）——materials 路径有两页，而
`resolveResumeTarget` 对该路径的默认落点是第 5 步 `/materials/processing`，只在第 4 步配链接
会让用户停在没有出口的页面；新增所选路径页面时必须同步配置该入口。
**不得为「更改填报方式」添加「原路径内容会丢失」一类确认提示**：后端
`update_report_primary_input_mode` 只改 `meta.primaryInputMode`，加该提示会让界面陈述与系统事实
相矛盾。生成前必答的议题问题只阻断 questions 路径；选 materials 的用户不被它们阻断——资料走
块级证据轨、不写回 `intakeItems`，AI 无法代填它们。该判定必须与导出闸同源，否则出现
「生成得了却导不出」。
**两条路径均开放图片素材**：图片素材区挂在所选路径末步页（materials 路径的
`/materials`、questions 路径的 `/intake/questions`，§3.5）；开放与否和扩展名白名单
只由服务端 `intake.policy` 投影决定，前端不硬编码。

不设独立准备概览页：新建/打开报告直接进入分步流第一步。**「资料处理」是导航上看得见的一步**
（`/materials/processing`，§7.3），不是点了提交才出现的隐形工序：用户在第 4 步就应知道后面还有
第 5 步。生成动作在所选路径末步页尾的**生成区**就地完成（唯一共享实现
`frontend/components/intake/generation-section.tsx`；materials 路径挂资料处理页尾，questions 路径挂
议题信息页尾）——内容为素材清单（「AI 将基于以下内容生成」+ 各输入域的服务端状态行）、生成阻断项
（链接到对应步骤）与显式生成/更新按钮；资料处理进行中时按钮置灰并给出置灰原因。生成区不单独成页：
生成是分步流的收尾动作，不是需要专门导航到的目的地；不设 `/prepare` 一类准备总览路由。

分步页遵循**渐进披露**：默认只渲染阻断生成的必填最小集，选填内容统一收进页尾「选填补充」
折叠区（唯一共享实现，标题行附一句话说明不填后果，展开后无二级折叠）。
**页头不写页面自述**：「AI 将据此生成…」类复述页面用途的句子一律不出现——页面标题与表单本身
已自明；仅当存在**不自明的事实**（留空后果、可跳过性、路径差异）时，用一句话补充该事实本身。
同一事实在同屏只说一次：字数边界只由计数器表达（`已输入 / 上限 · 至少 N`，§2.2.1）；节标题不与
字段 label 逐字重复（单字段节不设节标题）；「选填」由「选填补充」折叠区标题统一表达，区内各节
不逐个标注。界面语气取严谨、平静、权威，不寒暄不拟人。

**折叠区的可发现性**：渐进披露的前提是用户**看得出这里可以展开**，否则选填内容等同于不存在：

- **有边界**：折叠态整体是一个可点击行，带 `--surface-sunken` 底色与 `--border` 圆角边框；不是一段裸文字。
- **有可供性**：行首用 `▸ / ▾` 三角指示展开态（`› / ˄` 读作装饰而非控件），整行 hover 时底色转
  `--accent-subtle`，光标为 pointer。
- **中性配色**：标签文字用 `--foreground`，说明文字用 `--muted-foreground`；折叠区不是主操作，
  按 §2.1 不得使用 `--accent` 作为文字色。
- **说明常驻标题行**：一句话说明（不填后果）在折叠与展开态均显示；不罗列区内内容清单。

该形态由 `frontend/components/intake/optional-supplement.tsx` 唯一实现，任何页面不得自建
平行折叠样式。**基本信息页的选填补充默认展开**：折叠行位于长表单末尾、主操作动线在页脚
「下一步」直接绕过它，默认折叠会导致选填内容整体漏填；用户仍可手动收起，其余页面维持默认折叠。

步骤序列由 `getIntakeSteps(scope, primaryInputMode)` 根据当前报告 scope 与报告级填报方式动态决定。

**固定四步，所选路径的页面是第 4 步的二级条目、不是替代它的同级步骤**：

- **未选择**：基本信息 → 重要性评分 → 定量信息 → **定性信息**（无二级）
- **materials**：… → **定性信息** ├ 上传资料 └ 资料处理
- **questions**：… → **定性信息** └ 议题信息（含图片素材区）
- `materialAgentEnabled=false`：二选一不成立，但二级仍为「议题信息」——它是该步唯一可操作且承载页尾生成区的页面；二级为空会让「下一步」与 `generationEntryHref` 落到只承载路径选择的 gateway

**一级恒为「定性信息」且去向恒为二选一 gateway（即换路径入口），总步数恒为 4**：一级标签
不随路径漂移，用户始终看得出路径页面同属「定性信息」，并保有回到二选一页的导航入口；二级在
选定路径后常驻展开，不随当前停留步收起。二级高亮按最长前缀取唯一命中项——`/materials` 是
`/materials/processing` 的前缀，按「前缀即命中」会把两条同时点亮。`matchIntakeStepIndex` 与
`resolveIntakeStep` 一并消费二级 href：停在路径页时当前步仍是第 4 步，而「下一步」指向下一步的
**首个可操作页**（已选路径时直达路径首页，不折返 gateway）。

重要性评分步（`/intake/scoring`）开放评分收集并写入交付物（未评分按完整议题覆盖策略组织）。
该步的准备中心区块必须照常产出、可正常变为已完成，只是 `required_for_generation=false`
不阻断生成——「是否展示这一步」与「这一步能否完成」必须读同一侧事实，分别读收集侧与报告侧
会让该步永远停在未完成。

生成前必答的议题问题由议题信息填写页（`/intake/questions`）的"生成前需要确认"区承载，
不是报告正文页的任务：`topic_questions` 区块与相应阻断项一律指向该页。必答项之外的推荐项走页内
议题分组折叠区；答案直接写回 `Report.intakeItems` 并经全局自动保存持久化，完成门槛由服务端准备
投影拥有——`required_for_generation` 由当次实际解析出的议题义务决定，不由范围类型硬编码。
多选题带 `optionGroups` 时必须按组呈现：组标签 + "至少选择 N 项"约束可见（未达标用警示色、
达标转弱化色），组约束是合同判定（`input_obligations` 按组判定完成），不得以扁平清单隐藏。
该分组呈现由 `TopicIntakeField` 统一承载，分步页唯一渲染。

步骤位置在外壳左栏步骤导航展示（§3.0 `IntakeStepRail`，读 `getIntakeSteps(scope)`），步骤页面
正文区不再重复渲染步骤组。每步页面底部的上一步/下一步直连由唯一共享实现渲染（`frontend/components/intake/intake-step-navigation.tsx`
的 `IntakeStepFooter`，全量使用 §2.1 语义令牌），任何页面不得自建平行的步骤导航样式。intake 各步
页内不设保存/前进主按钮（编辑内容自动保存），footer「下一步」是每屏唯一主色主按钮、统一深绿主色
（§4 每屏唯一主按钮）。**例外**：议题重要性评分与定量信息是选填页，页首右上角
渲染与页脚完全相同的「下一步」主按钮（同一共享实现 `NextStepLink`，同样式、同措辞），明示这两页
可以不填完全部内容直接进入下一步；该两屏的「唯一主色主按钮」放宽为页首、页脚同钮。
末步（materials 路径的资料处理、questions 路径的议题信息填写）由页尾
生成区收尾、不经该 footer 的「下一步」；首步无上一步、左侧留空——不放「报告列表」折返，
左栏报告组「我的全部报告」已承担。编制页恒有报告上下文（§3.0），footer「下一步」始终是普通链接。

**Excel 双通道覆盖每一个填写页面**，共用同一形态与命名模式：一行两控件——
「下载{表名}模板」（`Button variant="secondary" size="sm"`）+「导入{表名}」（同形态的文件选择
label）；整行是拖拽上传区（`FileDropZone` 默认形态），拖入 .xlsx 与点选文件同路——都先经
ConfirmDialog 覆盖确认再执行导入，行尾以 supporting 字色提示可拖拽。唯一共享实现
`frontend/components/intake/workbook-channel.tsx`（导入前 persistNow 落定在线草稿，成功后以服务端
权威 state 重建本地报告）。表名与页面对应：

| 页面 | 表名 | 服务端 kind |
|---|---|---|
| 基本信息（步骤 1） | 基本信息表 | `report_basics` |
| 重要性评分（步骤 2） | 评分表 | `assessment` |
| 定量信息（步骤 3） | 定量信息表 | `quantitative_metrics` |
| 议题信息填写（questions 路径末步） | 议题信息表 | `topic_questions` |
| 报告列表 | 统一填报工作簿 | `unified_workbook`（整册：基础资料+评分+定量+议题问题；评分/定量整表留空=暂不提交保持现状） |

**统一填报工作簿是账户级整册通道，不属于任何分步页**：

- **模板下载全局只有一个入口**：报告列表页头「下载统一填报工作簿模板」（`secondary` 按钮，
  与「新建报告」同行；空状态下同样提供）。下载的是**账户级空白母版**——不绑定任何报告，
  可导入任意一份报告；账户快照加载中按钮置灰并说明原因。
- **导入每份报告一个入口**：列表行标题格下方 supporting 字色文字链「导入统一填报工作簿」，
  结果/错误按行就地展示。
- **导入必须先经 ConfirmDialog 确认覆盖语义**，全部 Excel 导入通道（统一册与分步页四类单表）
  统一措辞：「导入操作会覆盖报告中的现有内容。」确认按钮「导入」，destructive 形态。

分步页单表通道维持双通道形态；其模板按当前报告状态动态投影并**预填现有内容**（下载即状态
快照，导入为整批替换）。评分页的财务/影响阈值是在线判定参数，与 Excel 通道解耦、在其下方单独成行（`--surface` 底容器；
默认值说明收在 ⓘ，不作常驻文字）。

#### 3.2.1 双重重要性矩阵 · 四象限底色

矩阵的**四象限底色是构成性视觉语义，不是装饰**：它让读者在看散点之前就知道图被阈值十字切成
四个判定区，且以底色深浅表达重要性递减。缺了它，图退化成一片白底上的散点，读者无从判断
某点落在哪个判定区，而图例仍在解释一套画布上不存在的编码。

四象限底色一律由 `--chart-quadrant` 经 `color-mix` 派生四档透明度，**前端 SVG 预览、图例色块、
后端 `matrix_chart.py` 三处必须同源同值**：

| 象限 | 位置 | 分类 | 透明度 |
| --- | --- | --- | --- |
| 双重重要 | 右上（财务高·影响高） | `dual` | 50% |
| 财务重要 | 右下（财务高·影响低） | `financial` | 30% |
| 影响重要 | 左上（财务低·影响高） | `impact` | 28% |
| 非重要 | 左下（财务低·影响低） | `non` | 10% |

底色**常驻且不受图例过滤影响**——象限是坐标系的静态分区，不随可见分类增减而增删；动态过滤
只作用于散点与其标注（隐藏分类淡出保位）。二者是相互独立的两层，不得把底色也做成可过滤。
矩阵是页内一级区块（§7 重要性评分行）：自进入页面即在场，无散点时空画布同样呈现四象限底色
与阈值十字——底色在评分开始前就先行传达判定区语义。

**四象限必须等面积**：两轴各以阈值为中心取对称半径（贴合数据跨度并留边距），使阈值十字落在
画布几何中心。若改用数据 min/max 线性映射，象限面积随数据分布悬殊——铺上底色后，面积最大的
往往是「非重要」象限，视觉上暗示它最重要，与语义相反。前端 SVG 与后端 `matrix_chart.py` 用
同一算法。

散点颜色另有一套按分类的语义着色（`--accent`/`--ai`/`--warning`/`--muted-foreground`）：底色标
区域，散点标个体归属。图例色块表达的是**象限底色**，取上表色阶，不取散点色。**散点着色只在
前端 SVG 交互预览成立，不属于三处同源**：后端 `matrix_chart.py` 对全部散点统一取单色
（`_DOT_PURPLE`）——导出 Word 里的矩阵图是静态交付物，分类信息由紧随其后的重要性结果表承载。
三处同源只覆盖**象限底色**与**等面积算法**，前后端散点色不一致不是缺陷。

定量信息页单页全量渲染：指标 sheet（如经济+环境、社会、治理）作为一级分区标题，其下按
category 分组三线表；不做分部分翻页，页内不设保存/翻部分按钮。保存由草稿自动保存承载
（防抖整批提交，离开页面保底 flush）；数值留空的指标在保存时记为“尚未收集”，不阻断保存或
生成。界面不设“无数值原因”列；受控无值原因仍存在于数据契约与 Excel 导入路径。

数值与时间输入统一采用“草稿两层”约束：键入中间态（如 `3.`、`202`、`2026-0`）留在本地草稿，仅完整
合法值写回 Report；粘贴自动清洗千分位逗号、空白与全角字符，但不剥离单位或说明文字。

重要性评分与定量信息页的填写内容自动保存为草稿（防抖整批提交，服务端接受部分评分子集；进度与完成性
提示不受影响），用户离开页面或刷新不丢失已填内容。报告在其他窗口被更新时，页面以非阻断横幅提示并保留
本地未保存草稿，由用户显式选择加载最新内容；不做自动合并。报告路由之间的导航不重新加载报告。
错误提示必须传达服务端真实原因（固定引导语 + 具体原因），禁止用
"检查网络"类文案覆盖权限或校验类错误；页面内一律使用 role=alert 呈现，不使用浏览器 alert 弹窗。

只有服务端准备投影声明的最低企业身份信息、已上传文件的待补说明、以及解析失败的文件
（闸一，§7.3）阻断首次生成；重要性、定量信息和文件数量均可为空。页面不得用“资料齐全”暗示
所有上传文件都会进入正文。

报告正文页（§3.2）是生成成功后的阅读与修订面。放行门是「最近一次生成成功」（读
`fetchLatestReportGeneration`），未生成时渲染指向交付页的空状态；它不依赖 report profile 的
`workbench_enabled`——该开关只裁决服务端整节重写与模块标题端点，二者仍关闭。
分步准备、生成页、交付页与报告正文页构成完整闭环；导出阻断文案引导「补齐后重新生成」，
导出前检查在报告正文页打开。

### 3.2 报告正文页外壳

报告正文页服务于报告阅读、逐段修订与逐块溯源，不承担首次信息采集，也不承担局部重写
（§3.6 前置未解除）。布局为三区（§7 报告正文行）：

```text
[ 左：报告目录 ] [ 中：正文 ] [ 右：内容来源 ]
```

- 左栏只导航当前 Report revision 的实际章节（页内锚点），随阅读位置高亮，并显示生成就绪度。
- 中栏是结构化 Report 的 React 文档投影（`deriveDocument` → `DocumentRenderer`），约 680px 居中；
  投影不是正文 SSOT，可编辑段落的每次提交都是一条块级编辑动作写回 Report（§5）。
- 右栏「内容来源」是所选块的用户安全溯源投影（`BlockProvenancePanel`，读只读端点
  `block_provenance`）：内容依据、资料处置、填写与指标依据、生成记录、相对生成稿的差异；
  ≤900px 时改为抽屉。
- 右栏**不是「来源与报告助手」栏**，也不设该栏：任何助手形态都只能读取用户安全投影，不能看到
  内部 Prompt、成本、trace、私有工具策略或对象路径，也不能代替用户执行整份生成、导出或修改；
  溯源面板没有对话、生成或改写动作。

### 3.3 报告目录与状态

导航按 Report 实例实际存在的章、节和适用子主题装配，不把准备步骤、输入问题或固定 target 目录伪装为
报告章节。纯装配章节（`about_report`、`report_appendix`）自动从全局字段或结构拼装，不在导航中暴露。
章节可见性以服务端准备投影的企业及报告基本信息域（`report_identity`）为准，该域 ready 后章节树
可见，与生成区的「最低生成信息」判定同源。未就绪时「报告章节」组渲染一条引导入口（复用投影的
summary 文案并链接基本信息页），不得静默隐藏整棵目录；投影尚未到达或不可用（如本地开发模式）时，
浏览器以已填字段（企业名称与报告年度）作临时判定。状态只能来自服务端 typed projection；浏览器
不得根据文件数、轮询次数或页面访问顺序推断。

左栏是报告正文页专属的章节树：步骤导航与账户信息归全局顶栏（§3.0），左栏只保留
「报告章节」树与「报告生成与交付」「我的全部报告」两个常驻入口（与编制页左栏
报告组同职责，标签同源 `frontend/lib/report-navigation-labels.ts`），不渲染编号准备步骤组。章节树活跃项用主色浅底高亮；52px 窄轨保留收起形态。

- **已生成**：当前 revision 有对应内容并通过确定性结构诊断。
- **生成中**：当前报告运行明确正在处理该范围。
- **须留意**：存在客户可行动的 AttentionItem，但轻量版报告仍可交付。
- **可更新**：资料或结构化输入已变化，当前可下载 revision 仍保持有效。
- **暂不可用**：该范围发生明确技术失败，不能冒充“资料缺失”或 `context_only`。

### 3.4 输入与排版素材边界

企业基本信息、重要性评分、定量指标和语义文件都在分步准备中拥有唯一入口。报告正文页不采集也不改写
这些权威输入；需要修改时跳回同一 owner 页面，不创建章节内第二份答案。

`layout_asset`（证书、奖项、组织架构图等图片素材）上传后由用户写一句说明（10–140 字，复用文件说明
字段 `declaration.description`，不要求素材标题），再经独立的图片识别 Agent 做受控视觉识别，产出题注、
替代文本与放置范围；**用户说明是识别 Agent 选择放置范围的最高优先依据**。插入位置由系统按契约声明的
承载位确定性放置，用户在报告正文页预览放置结果、在资料入口页移除素材，不要求手工分类或选择插入位置。
**缺说明的 active 素材阻断生成，与语义资料同级**：没有说明的图片无法确定放置范围，
放错位置比不放更伤可信度。入队与重跑口径见 §3.5。素材一律以原图插入，不做结构化重绘。图片素材不进入语义 parser 与
Mapping；其识别摘要只服务于放置与题注。**例外是证书类图片**：识别判定为
证书时另按 `CertificateFact` 解析出名称、标准号、持证主体、认证范围、发证机构与有效期，
汇总到「可持续发展成果」章的集中承载表——证书是企业整体事实，一张 ISO 14001 天然横跨
环境、能源、污染物、废弃物、水资源等多个议题，集中承载使认证范围只写一次。任一字段辨认
不出即如实登记为不可辨认，不猜测；整张读不出则识别失败、按下条降级处置。某张图片识别失败不阻断报告生成：
该图跳过放置，资料处理页与 questions 路径的图片素材区显示「资料暂不可用」（§7.1）。报告正文页
溯源面板显示的语义资料来源由当前 Report revision 的 Mapping 决定与修订快照派生（`block_provenance`），
用户文件说明只作为理解辅助，不展示成“系统已确认事实”。

### 3.5 资料工作区与报告正文页的衔接

四类输入的服务端状态与生成资格由所选路径末步页尾的生成区展示（§3.1，不设独立总览页）。
用户点击“生成报告”或“更新报告”才创建新 revision，上传或 Agent 完成不自动改写正文。
资料变更后显示“报告可更新”，旧 Report 与 Word 在新版本成功前继续可用。

语义资料入口唯一在 `/materials`；图片素材入口在所选路径末步页（`/materials` 的图片素材区、
`/intake/questions` 的图片素材区），同一组件实现（`frontend/components/materials/file-intake-role-section.tsx`）。
不再使用 `/materials/profile` 或 `/materials/topics/*` 让 URL 替用户分类。每份文件使用服务端
`ReportFileIngressPolicy` 与 `UserFileDeclaration`：

- 每报告最多 30 份 active 去重文件（两类角色合计，合计上限在页头说明一次），单文件最多 10 MiB，
  PDF 最多 40 页且不可加密；扫描件/图片型 PDF 自动执行本地 OCR 文字识别后进入解析，用户无需额外操作；
- 上传区是两个独立拖放区「报告资料」与「图片素材」，**投放到哪个区即声明哪类角色**，
  不再由扩展名推断；每区各自按服务端 policy 投影的白名单预检，不支持的文件逐个具名即时反馈
  （「不是「{区名}」支持的格式（{扩展名}）」），不静默丢弃、不跨区改投；区内按钮「选择文件」用次要
  描边样式、始终可点（仅上传中禁用），点击即打开文件选择器，选择后立即整批上传并显示进度
  （6px 细条 + 百分比）；每区区头显示本区 active 份数与待补说明份数（`--warning` 色，0 不显示）；
- 每份文件在列表中以两个可直接编辑的填空呈现：文件名（展示名，可改，不改动原始文件、不影响已有
  分析与确认状态）与文件说明；无独立编辑态与保存按钮，失焦即保存，值未变化不产生服务端修订；
  未达最小字数的说明停留在本地草稿并就地提示"还差 N 字"，不提交；
- 资料处理由**说明补齐自动触发**，没有单独的「开始处理」动作——整条主链收敛到末步的单一
  「生成报告」按钮。说明未补齐时保持 draft 形态，**说明补齐前不消耗模型调用**（文件说明进
  `input_fingerprint`，上传即入队会白烧一次空说明的调用）。两类角色的入队口径不同：
  - 语义资料：最后一份说明保存成功的瞬间，服务端在同一次请求内入队 File Agent；自动入队失败不得
    连带用户已保存的说明失败，页面仍可经「下一步：资料处理」兜底重试。**调度权收敛到 confirm，
    remove/restore 是纯状态变更**。提交后新增、修改说明、移出或恢复语义资料都会使确认失效
    （服务端按 active 语义资料集指纹派生判定），重新入队时只重跑发生变化的文件；
  - 图片素材：说明保存成功即入队图片识别 Agent，**与资料集确认解耦**；图片素材的新增、修改、
    移出、恢复不影响确认状态。修改说明按幂等指纹重跑，移出不重跑，恢复沿用既有结果；
- 存在缺少说明文本的 active 文件（两类角色同计）、或语义资料集尚未确认时，准备投影分别给出阻断项
  （同一时刻只出现其一：缺说明优先）；缺说明的阻断项指向该文件所在入口页（materials 路径为
  `/materials`，questions 路径为 `/intake/questions`），补齐后方可生成；零文件路径不受影响；
- `semantic_material` 接受 PDF、DOCX、XLSX、PPTX，说明文本 10–140 字（上传时可暂空）；角色由用户
  投放的上传区声明；议题归属由 File Agent 从文件内容判定，不再提供议题标签选择；
- `layout_asset` 接受 PNG、JPG/JPEG、WebP 与单页 PDF（多页拒收并具名提示），不要求素材标题；说明
  10–140 字（上传时可暂空，补齐前不入队识别）；题注与替代文本由图片识别生成，题注改写入口随旧工作台退场
  （恢复路径待排期），改写后的题注不被识别重跑覆盖；
- 前端不得硬编码扩展名、字数、数量、大小或页数；策略、提示和校验均从服务端投影；
- 普通动作使用“移出报告”和“恢复”，不显示或调用物理删除原文件；失败文件不占 active 上限。

两个上传区的文案（代码从此处取，不得各自发明）：

| 区 | 标题 | 表头问句 | 说明 placeholder | 空状态 |
|---|---|---|---|---|
| 报告资料 | 报告资料 | 这份文件是什么、希望怎么用？ | 例：2024 年员工手册，用于劳工权益与员工培训相关内容（可稍后填写） | 上传制度、报告或数据文件 |
| 图片素材 | 图片素材 | 这张图是什么、希望放在哪类内容旁？ | 例：2024 年 ISO 14001 认证证书，用于环境管理体系相关内容 | 上传证书、奖项、组织架构图等图片 |

**两个区各带一句常驻用途说明**：区标题下方常显，不随是否已上传消失——两个区的内容进报告
的方式并不相同，只在空状态里说一次的话，已上传的用户就无从知道证书该放哪个区。

| 区 | 常驻用途说明 |
|---|---|
| 报告资料 | 文件内容会被逐份处理，作为报告正文的事实来源。 |
| 图片素材 | 图片按说明放进报告对应位置；证书类图片另会被识别出名称、发证机构与认证范围，汇总到「可持续发展成果」一章，无需再逐个议题手工填写。 |

questions 路径的图片素材区另带一句不自明事实（§3.1 页头规则）：「缺少说明的图片会阻断生成；不上传
图片也可以生成」。

文件说明与标签帮助 File Agent 理解用户意图，但不是企业事实或采用结果。用户上传并声明的文件默认
视为报告主体自有或经授权使用的企业资料：文中项目、案例与数据默认归属报告主体，系统不得因文档未写明
公司名称而质疑归属或要求用户"确认归属后方可使用"。界面不展示关键词命中、
“自动分类为某 Block”或预解析片段流水线；它展示服务端的候选相关性、Dossier 摘要、来源文件、
AttentionItem 与用户下一步。待确认事项不阻断生成，用户回答后再由服务端增量处理受影响范围。
文件处理状态使用三种用户语义：“资料已处理”“资料已处理，部分事项须留意”“资料暂不可用”
（§7.1：资料语义一律用「处理」不用「理解」，状态枚举值不变）；不得把少一个 Markdown 引用、
parser 内部提示或可恢复的局部限制统一显示为“处理失败”。暂不可用资料不进入新报告事实，
但不阻断其他资料和零文件路径。

`/reports/generation` 只轮询服务端报告运行投影。进度以用户可理解的当前处理对象、已完成结果、可行动
异常和是否可下载为核心；前端不得硬编码“解析—分类—映射”等阶段，也不得从文件数或轮询次数推断成功。
交付页提供当前 Report revision 的 Word、客户审阅包与「阅读并修订报告正文」入口。生成成功但存在导出阻断
（`export_blocked` 事件）时，交付页展示「需要你处理」待办，用户可在报告正文页打开「导出前检查」
逐项处理后导出正式稿，本次不提供交付物下载，也不显示“稍后刷新重试”。客户可见页面不提供内部审计包，
也不展示 Prompt、对象路径、成本、私有工具策略、debug 或模型思维。

报告正文页只做阅读、逐段修订与溯源；局部重写与素材插入不在其中（§3.6 前置未解除）。

报告正文页呈现的报告结构必须与生成侧权威装配同源：`/api/plan` 与 `build_report_revision` 对同一份
state 的重要性策略判定一致（无评分输入 → complete_coverage 全议题装配；有评分 → 按重要性结果），
两条路径给出同一棵章节树。议题章节（含生成正文、表格、素材承载位）因此在报告目录与正文区可见、
可编辑，与导出 Word 的内容一致；渲染层保持结构无关，不因议题数量特判。范围投影
产出的 Report 必须仍符合合同——非空字段（如 appendixPackage）投影为契约默认值而非 None，
前端 Ajv 对越界产物忠实拒收是合同门禁，不是需要绕过的障碍。
所有公共页面继续提供稳定 `data-screen-id`、`data-screen-label`、键盘焦点和文字化状态合同。

---

## 4. 组件 Components

> 原则：组件是「克制的默认值」，不是上限；缺省样式遵循本文，特殊场景在原则内扩展。

### 4.0 组件层清单（L2 原语）

以下组件必须有**唯一实现**，位于 `components/ui/`，只读 §2 令牌、不读业务数据：

`Button` · `FieldRow` · `Hint` · `Badge` · `StatusDot` · `InlineAlert` ·
`ConfirmDialog` · `Collapse` · `EmptyState` · `LoadingState` · `FileDropZone`

`FileDropZone`：拖拽上传区唯一实现——把任意区域变成文件拖放目标，拖入时 accent 高亮，drop 把
文件数组交回调；`dashed` 形态是常驻虚线拾取框（上传资料页），默认形态是隐形包装、仅拖拽悬停时
显示虚线描边（评分/定量页 Excel 通道行）。不承载上传逻辑与文件校验（权威校验在调用方与服务端）。

`globals.css` 不得存在 `.gs-material-*` 一类平行样式体系——它与 §9「Tailwind 类只承担布局与排版、
颜色一律引用语义 token」直接冲突，属于第三套颜色体系，其视觉职责全部由本层组件承担。

`globals.css` 保留：令牌声明、`body` 复位、`:focus-visible`、`@keyframes`、响应式断点，
**以及 §9 明确认可的受控类**——伪态（hover/focus）与关键帧动画无法用内联样式表达，
必须落在类里（如 `.gs-input`、`.gs-matrix-point`、`.gs-delivery-enter`、`[data-tip]`、`dialog[open]`）。
判据是「该样式能否用内联 `var(--token)` 表达」：能则不进 `globals.css`，不能（伪态、关键帧、
`::before`）才允许建类，且颜色仍只引用令牌。这与禁止 `.gs-material-*` 不矛盾——那套类
的问题是另建了一套颜色体系，不是因为它是类。
§4 原有条目继续作为各组件的细则。

### 4.0.1 Button

四级：`primary`（实心主色）· `secondary`（`--border-strong` 描边）· `text`（纯文字主色）·
`destructive`（`--destructive` 描边，**绝不实心**）。
尺寸：`padding: 9px 22px`（默认）/ `7px 14px`（小）/ `10px 24px`（大）；`--radius-control`；
`--text-body-size`；字重 500；禁用态 `opacity: .4`。

三条可机械检查的规则：

1. 一个路由的渲染树里 `variant="primary"` 至多出现一次。dev 环境运行时断言 + E2E 快照校验。
2. `disabled` 为真时必须提供 `disabledReason`，组件将其渲染为同行 `--text-supporting-size`
   `--warning` 文字。**置灰而不说明原因是主要困惑源。**
3. 破坏性操作必须走 `<ConfirmDialog>`，**禁止 `window.confirm` / `window.alert`**。

### 4.0.2 FieldRow

表单的唯一实现。槽位固定：`label`（`--text-label-size` / 500 / `--foreground`，**不允许 12px**）·
`required`（读 `field.required`，不硬编码；`--destructive` 星号）· `hint`（ⓘ，来自
`inputGuidanceAt()`，**可与 `inlineHint` 并存**但不得复述，二者内容逐字相同时 dev 报错；见 §2.2.1）· `inlineHint`（控件**之上**，`--text-supporting-size` / `--muted-foreground`，一句上限）· `control`
（`--text-body-size`；未填时边框 `--field-empty-border`，已填 `--border`，**禁止用背景色暗示
填写状态**）· `counter`（控件下方一行右对齐，`已输入 / 上限`，未达下限转 `--destructive` 并追加「至少 N」）·
`error`（控件**下方**独立行，与 `counter` 同行左对齐，`role="alert"`，`--destructive`）。

**版面顺序是 `inlineHint` 在控件之上、`error` 在控件之下，两者各占其位、互不吞没**——
先读怎么填再动手填，填错了在原地看到为什么。共用槽位会让报错把填写说明整段顶掉；该版面由
`FieldRow.layout.test.tsx`「说明在上、报错在下，互不吞没」钉住。

**表单控件禁用态**：输入控件禁用时整行 `opacity: .4`（与 §4.0.1 Button 同值），并在 `inlineHint`
槽位以 `--text-supporting-size` / `--warning` 陈述原因；`disabled` 为真而缺 `disabledReason` 时
dev 报错，各页不手写 opacity。已保存值在禁用态下照常回显，**禁用只关闭写入、不隐藏事实**——
控件消失而正文仍在引用该值，会让界面与交付物互相矛盾。能力尚未开放时用「暂未开放」，不写时间承诺。

适用面：内地包的披露准则收窄为沪、深、北三所指引三选一（准则由知识包固定的港交所两包
不渲染该选择器，改为陈述包固定的编制依据）；「港交所守则」与「其他准则、指南或官方文件」
因缺条款级映射禁用，文案「暂未开放。当前仅支持在所属准则包内编制。」字段、投影与既有值
一并保留，收资表不收集这两项。

- **按钮**：主操作主色实心（每屏通常一个；选填页顶底同钮例外见 §3.1）；次操作中性描边；文字按钮纯文字主色；破坏性用 `--destructive` 描边。禁用降饱和。文案用名词/动宾短语，不加感叹号。行内填空失焦即保存，不为每行设保存按钮；列表页的上传入口用次要描边样式，使"下一步：资料处理"保持唯一主色主按钮。
- **表单**：输入框 1px `--border`，聚焦转主色 + 3px `--accent-subtle` 焦点环；标签在上、克制留白；必填用 `--destructive` 星号（略大于标签字号并加粗，醒目；唯一实现 `FieldRow.requiredStarStyle`）。
表单页区块标题（H2）携带必填/选填标记（必填 `--destructive` 字、选填 `--muted-foreground` 字），
事实源与左栏二级条目同源（`lib/intake-info-sections.ts`，§3.0 二级标题）。单选/多选/步进器统一主色。
- **三线表**：表头 `--surface` 底 + `--muted-foreground` 字 + `--border-strong` 顶/底线；数值列等宽右对齐；默认行距偏宽，评分表/指标表才用紧凑模式。
- **结构化标签表**：利益相关方沟通表保持固定八行和三线表骨架，第一列只读；议题和沟通方式在单元格内以中性描边标签展示，点击后用克制浮层分组多选。选中态只使用松墨绿；自定义方式在对应类别末尾通过单行输入补充，不引入标签管理器、行业颜色、AI 按钮或模型加载态。
- **状态标签（Badge）**：高 20px，`padding: 0 8px`，`--radius-pill`，`--text-overline-size`，
  字重 500；语义色 10% 底 + 饱和文字（成功 `--accent-subtle`/`--accent`、生成中 `--ai-subtle`/`--ai`、
  待确认 `--warning-subtle`/`--warning`、阻断 `--destructive-subtle`/`--destructive`、中性
  `--surface-sunken`/`--muted-foreground`）。状态文字取自服务端投影的中文语义，**前端不自造措辞**。
- **状态表达三重编码（强制）**：**每一个状态必须同时提供颜色、形状与中文文字三个通道**（§2.7
  「不只靠颜色」的强化）。`StatusDot` 形状编码：已完成＝实心圆 8px `--success`；进行中＝实心圆 +
  脉冲光环 `box-shadow: 0 0 0 3px var(--accent-subtle)`（遵守 `prefers-reduced-motion`）；
  待确认＝**空心环** 8px `2px solid` `--warning`；未开始＝实心圆 `--border-strong`；
  暂不可用/硬阻断＝**三角** 9px `clip-path: polygon(50% 0,100% 100%,0 100%)` `--destructive`。
- **InlineAlert**：取代散落各处的裸色文字。结构＝左 3px 语义色竖规 + 语义色 6% 底（`--*-surface`）+
  `StatusDot` 形状图标 + 文字；`padding: 11px 14px`，`--text-label-size`，行高 1.75。三个 variant：
  `error`（`--destructive` / `--destructive-surface`）、`warning`（`--warning` / `--warning-surface`）、
  `success`（`--success` / `--success-surface`）。**错误提示不得降级为中性色**——登录失败等错误
  必须走 `InlineAlert variant="error"`，不得用 `--muted-foreground` 灰字。认证错误按凭据、账号状态、
  限流、网络、服务异常五类表达的要求（§7）不变。
- **进度**：细条（6px，`--accent` 填充）+ 百分比；用于整份报告与单步完成度。
- **浮层/工具条**：白底 + `--border` + 浮层阴影；选中正文时贴近选区出现，块级操作走就地按钮，不设常驻干扰阅读的工具栏。
- **反馈**：全局用 Toast；局部阻断用就地 Alert/内联提示。
- **生成占位**：报告正文不显示模板占位文字。尚未生成的 AI 段落只以块状态呈现，真实模型结果或用户编辑后才进入正文内容。
- **资料处理卡**：显示原文件名、用户说明、File Agent 的候选相关性、Dossier 摘要、建议使用程度和
  用户可理解的三态结果；不把 Markdown 拆成固定企业事实字段，也不展示关键词命中、parser 内部步骤
  或模型思维。“相关”明确解释为进入后续评估，不代表一定写入报告；追溯到原文件，不显示内部段落 locator。
- **待确认事项卡（尚无组件实现，实现时按此规格）**：用用户可理解的语言说明 `AttentionItem`，**必须包含五段：
  问题 · 原因 · 来源 · 影响 · 下一步**——缺任一段视为不合格状态提示。结构：`1px solid var(--border)` +
  左侧 3px 语义色竖规 + `border-radius: 0 12px 12px 0` + `padding: 16px 18px`。轻量版须明确标注
  「不阻断本次生成」或「阻断生成」；回答产生用户声明并触发受影响范围更新，不把回答伪装成原文件证据。
- **保存失败**：自动保存失败不得清空或覆盖本地编辑；全局外壳显示可操作的错误提示与“重试保存”。站内路由切换不得重新加载同一报告并覆盖防抖窗口内的编辑，只有认证、报告 ID 或合同生命周期变化才触发整份状态恢复。
- **正文新鲜度**：生成输入变化后保留现有正文并显示“输入已变化”；不得自动重写或消耗额度。用户显式成功重写后才恢复“与当前输入一致”，失败重写不改变正文或计数。
- **资料错误与澄清卡**：失败信息必须包含文件、失败阶段、领域原因和下一步操作，不直接展示
  `Failed to fetch`、`Connection error` 等浏览器或 Provider 原始错误；原始详情只进入受控观测。
  澄清优先给出可选项，并保留自由文本回答。普通移出必须说明“文件不再用于新报告，但旧版本仍可
  追溯”，并提供恢复入口；界面不提供永久隐私删除。
- **空状态（EmptyState）**：列表或区域无数据时，使用统一的空状态模式——图标（可选，线性单色）+ 一句话说明 + 主操作按钮（如有）。不使用插画、装饰卡片或营销文案。文案用动词引导下一步（如"新建一份开始编制"），不只说"暂无数据"。
- **加载状态（LoadingState）**：
  - 页面级加载：骨架屏（shimmer，`--surface-sunken` 底色 + 主色微光）+ 简短状态文字（"加载中…"）。
  - 内容级加载：内联 spinner（三点脉冲，主色）+ 状态文字。
  - 操作级加载：按钮内 spinner + 按钮文案变为进行中态（如"保存中…"）。
  - 不得只使用裸文字"加载中…"代替骨架屏或 spinner；不得重复"正在加载…"。
- **操作按钮分级**：
  - 主要操作（保存、生成、下一步）：实心按钮 `--accent` 底 + `--accent-foreground` 字，每屏至多一个（选填页顶底同钮例外见 §3.1）。
  - 次要操作（导入、下载模板、返回）：描边按钮 `--border-strong` 描边 + `--foreground-secondary` 字，或文字按钮 `--accent` 字。
  - 同一区域的主要操作必须使用实心按钮，不得降级为文字按钮。

---

## 5. 内容编辑模型 Editing Model（核心）

报告正文是结构化 Report 的**受控文档投影**（`deriveDocument` → React 节点），默认只读阅读；可编辑段落经双击或 Enter 进入就地编辑，每次提交是一条可撤销的块级编辑动作（`BlockTextEdit`）写回 Report，投影与编辑日志都不是真相源。下列视觉必须忠实反映 Schema（`Block.blockType` × `Block.state` × `source`）。

### 5.1 块的可编辑性 = 字色（颜色即权限）

由 `blockType` / `source` 决定，**用字色一眼区分，无需文字说明**：

| 情况 | 视觉 | 来源 |
|---|---|---|
| `fixed`（含监管强制表述）、`source` 为 derived/assessment | **灰色 `--muted-foreground`，只读锁定** | 投影不给锁定块编辑入口 |
| `slot`（固定句式 + 填空） | 句式灰/墨，**填空处绿色高亮**（§5.3） | — |
| `constrained` / `generative` / `source: user_input` | **墨色 `--foreground`，可编辑** | — |

锁定块可附极小「固定表述 · 不可编辑」标签，不抢戏。

### 5.2 块状态机视觉

状态取自 Schema（`pending → generating → ready → locked`，失败 `failed`）。生成结果与用户编辑后的有效内容统一为 `ready`，不存在段落、表格行或整表的“采纳”步骤。状态只出现在标注、底色或角标，绝不进正文：

- **generating**：柔和底色高亮（不用硬边框）+ shimmer 骨架 + 三点脉冲「正在生成…」。
- **图片承载位**：没有放置素材、也没有派生图或评估矩阵可绘的图片块不进入正文，不显示任何「占位」文字。
- **ready**：墨色正文；进入编辑时使用白底与清晰 `--ring` 描边，左侧 `--success` 小绿点表示已修改。
- **locked**：随章节或报告定稿，不再单块切换。
- **failed**：`--destructive` 提示，允许用户继续手工编辑或重新发起整节生成。

### 5.3 字段回填 = 绿色高亮（统一）

`Inline.ref` 引用的字段值，**预览侧与编辑侧统一用绿色高亮**（`--field-foreground` on `--field-background`），随左侧输入实时回填。未填占位也走绿色系（如 `【公司简称】`），**不用黄/琥珀**。

### 5.4 生成与标题语义

- AI 入口是服务端整节生成；用户更新输入后可在权益额度内重新生成整节，不能绕过账本调用单块、单行或整表模型接口。
- 生成结果直接成为当前可编辑版本并自动保存；不增加人工“采纳”操作。
- 左侧导航始终显示稳定 `Section.title`。正文预览、Word 与审阅 artifact 显示动态标题；H2/H3 固定不可编辑；H1/H4 的「编辑标题」与「保留当前标题」两项处理在导出前检查抽屉内按 `stale_display_title` 逐项提供（正文页标题旁的直接编辑入口仍随旧工作台退场，未恢复）。
- 动态标题过期时阻断正式导出。用户可重新生成、编辑标题，或选择“保留当前标题”按当前正文指纹重新确认；Planner、保存和导出不得隐式调用模型。
- 批量生成运行在导出闸前自动补齐本次运行产生的过期模块标题（仅过期时一次报告级调用，属生成运行既有授权，避免批量生成被自己的产出恒久阻断导出）；手动入口与用户三项处理（重新生成、编辑、保留当前标题）不变，Planner、保存和导出仍不隐式调用模型。

### 5.5 键盘流（零技术背景取舍）

借鉴 Jupyter 的「选中 / 编辑」双态与键盘流，但为非技术用户重做：

- **选中**：浅底（不加边线或竖条）；`↑↓` 在段落间移动，`Enter` 进入编辑。
- **正在编辑**：`--ring` 描边 + 左侧绿点；`Esc` 完成。
- 快捷键集中在**底部固定条 + 一处说明面板**，默认隐藏（`?` 唤出），**不在每段下重复**。
- 常用键：`↑↓` 选择 · `Enter` 编辑 · `Esc` 完成 · `⌘Z` 撤销 · `⌘S` 保存 · `?` 帮助。不提供段落重排（结构来自契约大纲）。
- **修饰键按操作系统自动适配，两套键位同等一级支持**：Mac 显示 `⌘ / ⌥ / ⇧`，Windows 显示 `Ctrl / Alt / Shift`（映射 `⌘↔Ctrl`、`⌥↔Alt`、`⇧↔Shift`、`↵↔Enter`）。进入时按系统自动检测并渲染对应符号，同时提供「Mac / Windows」手动切换；快捷键说明与底部固定条均随之切换。
- 取舍四条：① 键盘只是加速器，动作都有按钮 ② 颜色即权限 ③ 不用术语（「选中/正在编辑」而非命令态/编辑态）④ 说明只放一处。

### 5.6 AI 评审（预留）

AI 作为评审者产**只读批注**（缺失/不一致/疑似编造数字/披露缺失），归属 `sustainability_desk_ai`；与正文输入区分离，**不在输入表单里混入 AI 建议**。MVP 预留。

---

## 6. 内容与文案 Voice

- 用语完整专业、与数据契约术语一致；不用少见内部简称。
- 全角中文标点；面向用户的引导说清「现在做什么 / 为什么 / 下一步」。
- 按钮文案用名词或动宾短语，不加感叹号与营销修饰。
- **任何状态、提示、调试信息、provenance、红字元指令都不得写进报告正文内容。**
- **空状态文案**：用动词引导下一步（"新建一份开始编制"而非"暂无数据"）；说明原因时用中性语气，不暗示用户操作失误。
- **加载状态文案**：使用简短状态文字（"加载中…""正在生成…"），不得重复"正在加载…"或省略号堆叠。
- **登录页品牌**：登录页在表单上方居中展示文字字标（§2.10，`BrandMark`），不放图形标识；字标与标题之间保持 ≥16px 留白。

### 6.1 首次引导 Coach Mark

产品面向零 ESG 经验、零技术背景的用户。界面里有若干「看不出来的事」——ⓘ 的存在、某步可以
跳过、Excel 可以下载给同事填。这些不适合写成常驻文案（会加重层级坍塌），适合一次性指出。

**纪律**

- 每步**至多 3 条**，每条 **≤ 30 字**。
- 只讲「这个界面里你看不出来的事」，**不讲业务概念**（什么是双重重要性属于帮助中心，不属于 coach mark）。
- 每条锚定一个具体元素：目标元素加 `--accent-subtle` 光晕（脉冲动画 2s），深色浮层
  （`--foreground` 底 + 白字 + `--radius-container` + `--shadow-modal` + `padding: 16px 18px` +
  7px 三角箭头指向目标）呈现文字。
- 浮层内：overline「1 / 3」+ 13px 说明 + `[知道了]`（白底深字）+ `[跳过全部引导]`（弱化文字）。
- 提供「知道了」与「跳过全部引导」两个动作；后者一键关停。不提供「重新开启引导」入口（服务端 `onboarding_seen` 只记已见事实）。
- 遵守 `prefers-reduced-motion`：去掉光晕动画，保留静态高亮。

**每步教什么**

| 步骤 | 唯一要教的事 |
|---|---|
| 1 基本信息 | ⓘ 的存在与用法 · 选填补充折叠区 · 自动保存（3 条） |
| 2 重要性评分 | 这一步可以跳过，跳过的后果是什么 |
| 3 定量信息 | 可以下载 Excel 交给同事填，再导回来 |
| 4 上传资料 | 文件说明写得好，AI 才知道往哪儿用 |
| 5 资料处理 | 不需要——该页本身是自解释的结果页 |
| 生成 | 不需要 |

**状态存储**：`account.onboarding_seen[]`（服务端），**不用 localStorage**。换设备不重放，
清缓存不重来。

---

## 7. 页面语义注册表 Page Registry

统一、稳定的命名，便于人与 Agent 精确指代。每个页面卡片/根容器带 `data-screen-id`（= key）与 `data-screen-label`（中文名）。

| 中文名 | key (data-screen-id) | 路由 |
|---|---|---|
| 登录 | `login` | `/login`（邮箱 + 密码登录本机认证栈；未登录访问受保护路由重定向至此） |
| 报告列表 | `report-list` | `/reports`（登录后默认落地页，页标题「我的全部报告」派生自 `lib/report-navigation-labels`；空状态承载编制流程说明侧栏；纳入 `AppShell`。行为三列固定网格「标题 ｜ 完成度 180px ｜ 操作 308px」跨行严格对齐；行操作直接展示不折叠：已生成行「查看报告」+「查看输入」，其余行「继续/打开」；「删除报告」为行内 text 按钮（`--destructive` 字色）经 `ConfirmDialog` 确认。页头「下载统一填报工作簿模板」唯一全局下载入口（账户级空白母版），每行标题格下方「导入统一填报工作簿」文字链（§3.1 账户级整册通道），导入先经覆盖确认，结果与错误按行就地展示） |
| 根路径（无独立页） | — | `/` 无当前报告时重定向到 `/reports`；有当前报告时由 `resolveResumeTarget` 解析出**该继续的那一步**并跳转（服务端 `preparation` 的 `href` 优先，全失败回落 `/intake/info`），见 §3.0 |
| 基本信息（步骤 1） | `report-config` | `/intake/info`（企业及报告基本信息。单列正文，不设页内左栏「本页内容」（步骤导航在外壳左栏）；信息分区靠节标题+分隔线+留白；节标题携带必填/选填标记，与左栏二级条目同源（§3.0 二级标题）。页头只保留完成计数+进度条，不写页面自述（§3.1），不设「待完成」chip 行；选填补充默认展开（§3.1）。「新建报告」点击即创建服务端报告后进入本页（§3.0）；显示名派生为「轻量版报告 · {主体名称}」，未填主体名称时回落「轻量版报告草稿 · {创建日}」） |
| 重要性评分（步骤 2） | `materiality-scoring` | `/intake/scoring`（双重重要性矩阵是**页内一级区块**：它承载方法论专业性，不是填完才出现的附录。**上下堆叠：矩阵在上、评分表在下，各自独占整幅内容宽**，不得左右并排：22 个议题各两个输入框才是这一页的主任务，并排会把评分表挤进窄栏、并让矩阵列宽与外壳宽度耦合；堆叠后输入区拿到全部可用宽。矩阵尺寸须克制（SVG 360px）并与图例在面板内并排：它是随填写更新的参考图，不得占满首屏把输入区挤到折线以下。几何回归由 `e2e/layout-screenshots.spec.ts` 断言矩阵面板底边不越过首个评分输入框顶边；自进入页面即在场，**空画布常驻**——四象限底色与阈值十字先行呈现（§3.2.1），散点随填写增量落位（新散点一次性入场动画 `gs-matrix-point`，§2.6）。矩阵是**前端 SVG 交互预览**：图例选择即时淡出对应分类散点（保位不移除）、阈值调整即时移动分界线并即时重分类预览散点；全部可见散点就地标注官方议题名——贪心八方位候选布局，标签不得互相重叠或压住散点，过滤后剩余标签自动展开；hover 出议题名与两维得分。**增量预览分类与权威分类分两层**：预览走前端镜像规则（`classifyPreviewMateriality` ↔ 后端 `assessment_classify.classify_materiality` 同源，测试守护）；权威分类永远是服务端 resolved——评分完整并保存后回填，页内切换为权威展示（分类摘要 + 清单 + 一次性强调动画 `gs-matrix-reveal`，完成态回访不重复）。图例计数随预览散点实时，完成后与权威计数一致（规则同一）。工作台块预览与 Word 导出仍用后端渲染源（`/api/assessment/matrix`），所见即所得边界在导出） |
| 定量信息（步骤 3） | `quantitative-intake` | `/intake/metrics`（步骤标题「ESG 定量信息」） |
| 定性信息（步骤 4·未选择时） | `input-path` | `/intake/input-path`（步骤名与「定量信息」相对应，标题旁带「二选一」备注；报告级二选一 gateway：上传现成资料 / 直接回答问题。两个选项卡按钮（aria-pressed 形态，同 PathOptionButton 视觉），选择即保存并进入所选路径第一页；已选后不占步骤条位，由两条路径页顶「更改填报方式」低调链接进入更改。无资料解析能力时直接跳 `/intake/questions`） |
| 议题信息填写（questions 路径末步） | `topic-questions` | `/intake/questions`（生成前必答的议题问题平铺在页顶「生成前需要确认」区——它们阻断生成，不得藏进折叠分组；其余按报告章节树顺序渲染议题分组折叠行（▸/▾ + sunken 底 + 已回答 N/M 计数），组内 `TopicIntakeField` 平铺、选填、不设完成门槛；议题分组之后、生成区之前是「图片素材」区（questions 路径唯一的素材入口，只开放 `layout_asset`、不支持语义资料，`materialAgentEnabled` 时渲染，§3.5）；页尾承载生成区（与资料处理页同一共享实现），图片识别进行中时按钮置灰并给出置灰原因） |
| 报告正文 | `report-document` | `/reports/document`（生成成功后阅读整份报告、逐段修订并查看逐块来源；放行门是「最近一次生成成功」，未生成时渲染指向交付页的空状态。三区：外壳左栏**报告目录**（`ReportDirectory`，页内章节锚点，随阅读位置高亮）｜ 中栏**正文**（`DocumentRenderer`，约 680px 居中，段落双击或 Enter 进入就地编辑，每次提交是一条可撤销的块级动作）｜ 右栏**内容来源**（`BlockProvenancePanel`，所选块的溯源投影，≤900px 改为抽屉）。工具栏承载保存态、「撤销上一处修改」与「导出 Word」（导出前检查抽屉同源于服务端诊断）。**右栏不是「来源与报告助手」栏**：它只读用户安全投影，没有对话、生成或改写动作） |
| 上传资料（步骤 4） | `materials-workspace` | `/materials`（**纯上传与说明**（阶段 1）：两个上传区「报告资料」「图片素材」（§3.5），投放即声明角色；文件声明、上传、移出/恢复；不显示任何处理结果。说明补齐即自动入队解析，页尾以 supporting 字色告知「资料已在处理中」；主按钮「下一步：资料处理」此时只是导航，并兜底重试未成功的入队） |
| 资料处理（步骤 5） | `material-processing` | `/materials/processing`（分步流第 5 步；同页三态：未提交 / 处理中 / 结果；承载闸一判定与页尾生成区） |
| 生成与交付 | `report-generation` | `/reports/generation`（服务端进度、Word、客户审阅包与报告正文页入口。**不依赖报告编辑上下文**（REPORTLESS）：旧契约报告的已生成产物仍可查看下载，契约升级拦截页提供「查看已生成报告」入口。页内面包屑「我的全部报告 / 报告生成与交付」承担列表返回入口（该页无左栏）。运行记录是**定高滚动框**（约 300px）自动跟随最新事件、用户上翻查看历史时暂停跟随；运行中默认展开、终态默认折叠——页面主体不随事件增多变长。**版面顺序由运行状态派生**：成功且有客户交付物时，「下载交付物」区升到页首（页头之下第一区，一次性进入动画 `gs-delivery-enter`），进度、待处理与运行记录随之下移——生成完成后交付物必须在用户眼前，不要求下滚，也不用弹窗承接；成功但零交付物（导出闸阻断）维持原序，「需要你处理」保持在交付区之前。交付物卡片带文档图标（Word/审阅版徽标区分）、成功色底与主色描边，标签归 `artifactLabel` 单源） |

本表各行的
命名同时 human-readable（中文名）与 agent-readable（key/路由，源自代码路由），三者一一对应、不漂移。

- **认证页面形态**：登录页保持约 360px 的极简居中单列，只有邮箱 + 密码，不使用第三方登录、插画或装饰卡片；账号由后端命令建立。
- **权益与入口**：前端以 `GET /api/account` 与每份 Report 的 capabilities 决定入口，不按权益名硬编码分支；生成区禁用原因必须如实陈述真实原因（权益状态、达上限、资料处理中），不得回退为「生成前还需完成必填信息」一类误导文案。
- **报告类型**：建报只提交产品实体 `report_type=lightweight`（默认 Profile `sse_zh_hans@1`）。报告内容中的 `disclosureProfile.disclosureDetailLevel` 属于内容裁剪配置，不得作为权益或产品类型真相源。
- **报告正文页寻址**：所有报告节点都是 `Section`，统一以页内锚点 `/reports/document#section-<Section.key>` 定位；H1 用 `reportModuleId`，H2 用 `reportSectionId`，不通过字符串相等推断实体类型。标题元素携带 `data-section-key`，块容器携带 `data-block-id`，问题跳转与目录高亮都据此定位。
- **资料与运行状态必须可读可操作**：文件关系使用 `active | removed | superseded`；Agent 与生成任务使用
  `queued → running → succeeded | needs_attention | failed | superseded`。每个状态同时提供中文文字、处理
  对象、结果和有效下一步，不只靠颜色；`removed` 的下一步是“恢复”，不是“重新上传被删除文件”。
- **轮询与焦点**：进度刷新不得抢夺输入焦点、清空声明草稿或覆盖更新中的本地表单；浏览器只按服务端
  sequence 单调合并 typed projection。任何助手或抽屉都不得展示模型思维、内部路径、成本或 debug。

### 7.1 资料处理的用户语义

资料语义**全线用「处理」不用「理解」**（状态枚举值与展示文案分离）：

| 语义 | 文案 |
|---|---|
| 文件处理成功 | 资料已处理 |
| 文件处理成功、存在须留意事项 | 资料已处理，部分事项须留意 |
| 文件处理失败 | 资料暂不可用 |
| 进行中 | 正在处理… |
| 图片识别完成 | 图片已处理 |
| 上传页主按钮 | 下一步：资料处理 |
| 处理中的生成区提示 | 资料正在处理，完成后即可生成。 |

理由：「理解」是对模型能力的过度承诺。用户会据此推断系统真的读懂了内容，并在报告出错时归因于
自己「没写清楚」。「处理」只陈述事实，与 §0 的「克制、可信」调性一致。文案落点：服务端投影文案
（`material/workspace.py`）与前端状态文案映射，**状态枚举值本身不变**。

### 7.2 不承诺无法预测的量

界面**不得给出报告篇幅估计、生成耗时估计或填写时长估计**。页数取决于议题数、资料量与填写完整度；
耗时取决于队列深度与模型延迟；填写时长取决于企业规模与资料完备度。失准的承诺直接损害 §0 的
第一目标（可信赖性）。

替代方式：陈述**当前正在处理什么**（服务端已有 `current_object`）与**已完成 N / M 项**，
不折算成时间。

### 7.3 资料阶段模型与两道闸

资料环节存在三种性质不同的东西，不得混在同一个界面里：**用户的填写表单**、**提交处理这个动作**、
**处理动作的执行结果**。

**三阶段**

| 阶段 | 性质 | 内容 | 允许的文件状态 |
|---|---|---|---|
| 1 上传与说明 | 用户态 | 加文件、写说明、改名、删除，可反复编辑 | 上传中 / 已就绪 / 待填说明 |
| 2 解析与处理 | 系统态 | 说明补齐后自动开始；逐份解析可读性 | 等待中 / 正在处理 / 已处理 |
| 3 结果与修复 | 用户态 | 逐份给出结论；失败项必须处理掉 | ok / failed / ok_with_attention |

**阶段 1 不得显示任何处理结果。** 执行结果一律留到阶段 3。唯一例外：questions 路径的
图片素材区是该路径唯一的素材页、没有后继的处理页，行尾显示服务端 `image_analysis` 状态词（§7.1 三种
用户语义：正在处理… / 图片已处理 / 资料暂不可用），失败的图片必须在此可见；`/materials` 的上传区仍不
显示处理结果。

**两道闸**

| 闸 | 层级 | 判定 | 是否阻断生成 | 形状编码 |
|---|---|---|---|---|
| 闸一 · 解析阻断 | 文件层 | 加密、损坏、纯图无法 OCR、超页数 | **是（硬）** | 三角 `--destructive` |
| 闸二 · 内容须留意 | 内容层 | 文件可读但某字段未提取到 | 否（软） | 空心环 `--warning` |
| 资料集未确认 | 批次级 | 说明已齐但未提交处理（`materials.unconfirmed`） | **是（硬）** | 指向资料页 |

**「处理资料」与「生成报告」是两件事。** 存在闸一失败项时不允许进入生成——这是文件层面的
机械事实，用户的解法只有重新上传或移出报告。两类阻断不得混在同一个列表里。服务端按这两层
拆开投影：批次级 `phase`（draft / processing / reviewed）与文件级 `parse_status`
（ok / failed / ok_with_attention）+ 失败原因文案。

**闸二卡片的须留意事项必须结构化呈现，不得堆成连续文本**：卡片头一次性声明
「已处理 · N 项须留意」Badge 与「不影响生成」（supporting 字色，不逐条重复进正文）；
`attention_items` 逐条编号（`--warning` 色序号）分行——情况说明一行（label 字色），
「提示」+ 下一步动作独立一行（supporting 字色，「提示」词 medium 字重），条目间细分隔线。
`attention_items` 为空时 Badge 只显示「已处理」，不得凑数显示「1 项」。
页面形态由服务端 `phase` 决定，前端不推断；刷新、换设备都回到正确形态。

**措辞不用「待确认」「建议」**：系统中不存在与之对应的确认动作或待办入口。这批事项只是告知
资料本身的情况（口径打架、时点超出报告期、引用了未随附的文件），故统一用「须留意」与「提示」。
同一批事项同时投影到审阅稿「资料处理说明」前置页（§7.5），页面与 Word 两处措辞必须一致。

**范围外资料只加一行说明，不加第四种状态**：资料已处理、相关，但适用范围全部在本次报告之外
（上传了未纳入报告的议题资料）时，仍按 ok / ok_with_attention
呈现，卡片内多一行 supporting 字色说明（服务端 `file_analysis.dossier.report_scope_notice`，
文案按执行范围生成，主语是议题范畴而非这份资料）。
审阅稿封面说明用同一句按文件名列出，不得笼统说成「未采用」；其他未采用的资料同样按稳定处置 code 分组给原因
（`report_review_packages.MATERIAL_NOT_ADOPTED_EXPLANATIONS`：与企业事实呈现无关 / 存在须留意事项 / 未找到可直接支持正文表述的内容），
File Agent 与 Mapping 的模型文本不进入封面说明。

### 7.5 资料处理说明（审阅稿前置页）

审阅稿在**封面之后、目录之前**承载「资料处理说明」页，逐份列出来源文件名与该文件的
须留意事项、提示；内容由 `MaterialProcessingNotice` 从与正文批注同一份冻结 `dossiers` 投影，
条目文本原样取自 `AttentionItem`，不在导出侧重新措辞。

标题走普通段落而非 Heading 样式——多级编号与目录域只收录 Heading，因此该页**不占「第N章」章号、
不进目录**，正式稿与审阅稿的正文、章号、目录逐条一致。无须留意事项时整页不出现；正式稿一律不含该页
（与批注、审阅页脚同一道守卫）。版式参数归知识包格式 profile 的 `material_processing_notice`，
不在渲染器硬编码。
页内只有「资料核对事项」一节，按来源文件列出须留意事项与提示。

审阅稿批注的覆盖政策同页可见的原则一致：批注只出现在系统做过判断的单元上——模型撰写的正文与每一幅实际渲染的图
（素材图、指标摘要图、评估矩阵图各有具体出处说明）；模板固定文本与填写值的确定性投影不批注，
「关于本报告」「关于公司」两章正文整体不批注（豁免名单与理由见 `report_review_packages.COMMENTARY_EXEMPT_SECTION_KEYS`）。

### 7.4 生成期间离开页面

生成**不在浏览器里运行**。`createReportGeneration` 只是入队；实际执行在独立进程，由带租约的
worker 领取（`persistence/lightweight_report_generations.py` 的 `worker_id` + skip-locked 领取，
与 `material/agent_worker.py` 同一套模式）。`/reports/generation` 只轮询只读投影，无 `beforeunload`
拦截。

**结论：可以写「可以关闭页面」。** 但前提是完成后用户有处可看——这句文案必须与报告列表行内的
「生成中 / 已生成」Badge 同步上线。「有处可看」当前由三处共同承担：成功态交付物区升到页首
（§7 生成与交付行）、编制页左栏报告组「当前报告交付物」与报告列表行内状态。

---

## 8. Do's & Don'ts

**应做**
- 用留白、字号、字重表达层级与分组。
- 主色集中在唯一主操作、活跃态、进度与回填字段高亮（绿）。
- 报告正文用衬线、两端对齐、行高 1.95、列宽约 680px。
- 可编辑/锁定靠字色区分（墨/灰），回填字段绿色高亮。
- 块的状态用边线/底色/角标表达；用语完整、与数据契约一致。
- 空状态、加载状态和操作按钮使用统一的反馈模式（§4）；主要操作必须是实心按钮。
- 登录页展示文字字标，不放图形标识（§2.10、§6）。

**不应做**
- 不用「白底 + 阴影 + 圆角卡片」堆叠正文块——那是仪表盘语言，不是文档语言。
- 不给正文加背景色或彩色装饰；语义色不进正文。
- 不用渐变、彩色图标、插画背景、纹理。
- 不用横线切割内容（特殊分隔节除外，用极浅 `--border`）。
- 不对中文正文用 `letter-spacing`；不加载 CJK Web 字体。
- 不把状态/提示/调试写进报告正文。
- 不出现面向用户的「重新生成」按钮；不用黄/琥珀表示待填字段。
- 不堆叠多余字段、说明与嵌套——保持扁平克制。
- 不只用裸文字"加载中…"代替骨架屏或 spinner。
- 主要操作（保存、生成）不降级为文字按钮。
- 空状态不只说"暂无数据"，须引导下一步操作。

---

## 9. 治理与真相源 Governance

- **本文 = 视觉/交互真相源**；`docs/schema-contract.md` = 数据/行为真相源。视觉必须忠实反映 Schema 概念，但不重复定义业务语义。
- 二者一致性优先；如本文与现有前端实现冲突，**以本文为准并重构实现**（避免技术负债）。
- 触及令牌（token）、公开组件契约或会影响实现的视觉决策，先按 `CLAUDE.md` 给出具体改动文本并等待批准，再落地。
- **Token 引用语法**：前端一切颜色引用 §2.1 语义 token，禁止 Tailwind 调色板色阶（`neutral-500`、`text-white` 等）与十六进制字面量；`var(--token)` 后不写死 fallback。静态颜色倾向内联 `style` 写 `var(--token)`，Tailwind 类只承担布局与排版；悬浮、聚焦等伪态复用 `globals.css` 受控类（如 `.gs-input`）或以 Tailwind arbitrary value 引用 token（如 `hover:text-[color:var(--foreground)]`），且该元素的静息态颜色须同样写成类——内联样式会压制伪态类。跨页共享的类字符串常量（如 `accountInputClass`）同理允许 arbitrary value。
- 不维护独立的可视化样例稿（`Reference/design_system/` 一类需要随本文同步维护的平行副本已删除，
  不再建立）；对齐顺序为 design.md → 前端实现，遵从性由 §9.2 自动检查
  与 `lib/design-compliance.test.ts` 静态合同守护。一次性设计探索稿放交付包，不作为常驻对齐对象。
- 前端品牌位按 §2.10 执行，不得自造图形标识或产品名拼写。

### 9.1 SSOT 四层结构，单向依赖

| 层 | 位置 | 职责 |
|---|---|---|
| **L1 令牌** | `globals.css :root` | 唯一色值/字号/间距/圆角/阴影出处，逐条对应 §2 |
| **L2 原语组件** | `components/ui/*` | 只读 L1，不读业务数据 |
| **L3 外壳与模式** | `components/shell/*`、`components/intake/*` | 组合 L2，读服务端投影，不写死步骤数、字段清单或文案 |
| **L4 页面** | `app/**/page.tsx` | 只做数据编排与组装 |

**依赖只能自上而下。** L4 页面文件内出现 `style={{ color / fontSize / padding }}` 即视为需要
下沉到 L2 的信号——**这是信号不是禁令**（真正的硬线是 §2.1.2 的字号字面量禁止与 §9.2 的
自动检查）。判断标准是「值从哪来」而非「写在哪一层」：引用令牌的版面组装可以留在 L4，
出现字面量则必须下沉或补令牌。颜色与字号已有令牌阶；间距尚无令牌阶，`padding` 字面量是
同类的待办。

### 9.2 七条自动检查

| # | 规则 | 实现 |
|---|---|---|
| ① | `app/` 与 `components/` 中禁止十六进制色值 | 正则 `#[0-9a-fA-F]{3,8}\b`；`globals.css` 与品牌 SVG 豁免 |
| ② | 禁止引用未声明的令牌 | 抓取所有 `var(--x)`，与 `:root` 声明集求差 |
| ③ | 说明式文字禁止 11–14px 字号字面量 | 必须用 `--text-body/label/supporting/overline-size`；有只许收缩的豁免清单 |
| ③b | 标题禁止 15px 及以上字号字面量 | 必须用 `--text-title/section/subsection-size`；**无豁免清单**，仅报告正文 serif 排版面显式登记 |
| ④ | 禁止 `window.confirm` / `window.alert` | 正则 + ESLint `no-restricted-globals` |
| ⑤ | 每屏唯一 `variant="primary"` | dev 运行时断言 + E2E 快照 |
| ⑥ | 置灰 `Button` 必须带 `disabledReason`（§4.0.1） | 静态扫描 `<Button … disabled` 无 `disabledReason` 即失败；dev 运行时断言兜底 |

**规则 ③b 的豁免清单为空是刻意的**：标题带没有存量可赦免，留空清单才守得住；11–14px 带的
豁免清单只许收缩。把 SSOT 写成文档只是第一步，让它可被机器验证才是治理。

### 9.3 后端边界

视觉层不改后端写入路径、状态机与 Schema 语义，只消费既有投影：`preparation`（区域状态、
阻断项、`report_state_seq`）、`capabilities`、`intake`、`report_generation`、`block_provenance`（报告正文页溯源面板，稳定 code + 代码侧文案）。视觉层需要的服务端
事实一律以只读投影增补（如 `account.onboarding_seen[]`（§6.1）、批次级 `phase` 与文件级
`parse_status`（§7.3）、生成区的剩余额度提示、资料状态文案（§7.1）），前端不自行推断。
